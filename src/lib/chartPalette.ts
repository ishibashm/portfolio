/**
 * 分析画面（market・purchase・timing・admin/metrics）の図の決めごと。
 *
 * 同じサイトの中でグラフごとに色相が違い（teal・indigo・emerald・rose が
 * 1 頁に同居）、色に意味が無かった。系列の色は次の 4 つだけにする。
 * 新しい色を足さない。
 *
 * これは同格 4 色のカテゴリパレットではない。青の 3 段は「読み手が追う
 * 優先順位」で、neutral は系列ではなく参照。だから使い方に条件がある:
 *
 * - primary / secondary の 2 本までは線でよい（CVD 分離 ΔE 17.9 で合格）
 * - tertiary は白地でコントラスト 1.6 しかない。**細い線に使わない。**
 *   幅のある塗り（棒・面）に限り、直接ラベルを併記する
 * - neutral は生データ・参照線・比較対象の「地」。**それ自体を主役に
 *   しない**（灰色に読める色で、そう読ませるのが役目）
 * - 参照・補助の線は破線（REFERENCE_DASH）。実測の主系列は実線
 * - 系列が 2 本を超えるなら、色を増やすのではなく図を分ける
 */
export const SERIES = {
  /** 主系列。読み手が追う 1 本（実線・太さ 2）。 */
  primary: "#1e40af", // blue-800
  /** 比較の 2 本目。 */
  secondary: "#3b82f6", // blue-500
  /** 3 つめ。幅のある塗り＋直接ラベル限定（上の注意を読むこと）。 */
  tertiary: "#7dd3fc", // sky-300
  /** 生データ・参照線・「その他」。主張しない灰。 */
  neutral: "#78716c", // stone-500
} as const;

/** 目盛り線。データより必ず薄く。 */
export const GRID_STROKE = "#e7e5e4"; // stone-200

/** 参照・補助線の破線パターン。主系列は実線のまま。 */
export const REFERENCE_DASH = "4 4";
