# 改善の提案書（2026-08-12 時点）

利用者からの指摘をもとに、コードを確認して整理したもの。
**ローカルの Claude Code で回すことを前提に、1 項目 = 1 PR で完結する粒度**にしてある。

進め方は `CLAUDE.md` に従う（1 PR = 1〜3 ファイル、検証コマンド、判定ロジックの扱い）。

---

## 0. 先に確認した事実

推測ではなく、実際にコードを読んで確かめた。**再調査しなくてよい。**

| 事実                                                                   | 根拠                                                                                                                                 |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 俯瞰の県の色は**掲載件数**。吉凶とは無関係                             | `ArbitrageMapInner.getDensityColor` が塗るのは `prefecturesWithData.listingCounts`                                                   |
| 俯瞰の**吉凶モードは既にある**（既定 `overviewTint = "kigaku"`）       | `ArbitrageMapInner.tsx` の県 GeoJSON、左下に切り替えボタン                                                                           |
| 吉凶モードが**出発地か生年月日が未入力だと黙って件数モードに落ちる**   | `dayKigaku` が `if (!hasBaseLocation \|\| !birthDate \|\| !targetDate) return undefined`（`arbitrage/page.tsx:2032`）                |
| 75 日で基点が移る規則は**実装済み**                                    | `simulator/page.tsx:682` `if (step.purpose === "MIGRATION" && stayDuration >= 75)`                                                   |
| 距離は**利便性としてしか見ていない**。方位の作用の強弱には使っていない | `arbitrageScoring.scoreProximity`（5km 以内 100 点 → 50km で 0 点）                                                                  |
| 段階の色と語彙は 1 か所に集約済み                                      | `utils/tierDisplay.ts`（`TIER_FILL` / `BLOCKED_FILL`）                                                                               |
| 暦カレンダーは**2 実装ある**                                           | `widgets/CosmicCalendar.tsx`（`/calendar` と ホーム=`SolarTimeClock` が使う）と、`relocation/timing/page.tsx` が持つ独自のカレンダー |

---

## 1. すぐ着手してよいもの

判定ロジックに触らない。`CLAUDE.md` の「やってよいこと」の範囲。

### A. 俯瞰が黙って別の意味の色に落ちるのを直す — **完了（#147）**

以下は着手前の記述。**受け入れ条件は満たしている**ので、やり直す必要はない。

実際に入ったもの。

- `prefKigaku` が無くてもパネルを消さず、**理由つきで**「いまの色は掲載件数です」を出す
- 「方位の吉凶」ボタンは条件を満たさないとき disabled にし、理由を `title` に出す
- 件数の温度計の見出しに「吉凶ではない」を足した
- 実際に塗っている側を `effectiveTint` として 1 か所に置き、ボタンの強調・凡例・
  県の塗り・温度計をすべてそこから引くようにした（選択と実際の塗りが食い違うため）
- `__tests__/overviewTintLegend.test.tsx`（6 件）。旧実装に戻すと落ちることを確認済み

**次にやるなら B。**

---

<details>
<summary>着手前の記述（残す）</summary>

**優先度：高。**いま一番実害が大きい。

出発地か生年月日が入っていないと、県の塗り分けが「方位の吉凶」から「掲載件数」へ**無言で切り替わる**。
どちらも同じ場所を色で塗るので、利用者は**件数の色を吉凶だと誤読する**。

やること。

- `prefKigaku` が undefined のとき、俯瞰の凡例に「生年月日と出発地を入れると方位の吉凶で塗り分けます」と出す
- 件数モードで表示していることが分かる見出しを凡例に付ける（今は色バーに「件数」とだけ）
- 切り替えボタンの「方位の吉凶」を、条件を満たさないときは disabled にして理由を出す

触るファイル：`src/components/ArbitrageMapInner.tsx`（＋必要なら `arbitrage/page.tsx` から条件を渡す）

**受け入れ条件**

- 生年月日が空のまま俯瞰にしても、色が何を意味しているか画面から分かる
- 判定・色の計算そのものは変えない

</details>

---

### B. 時期ヒートマップに段階フィルターを付ける — **完了（#154）**

段階 6 つ＋天中殺の 7 区分で絞れるようになった。暦注（天赦日・一粒万倍日）は
別トグルで AND。外れた日もマスは残し、0 件のときは「該当なし」を出す。
絞り込みの判定は `src/lib/timingFilter.ts` にあり、テストが 17 件付いている。

<details>
<summary>着手前の記述（残す）</summary>

利用者の要望そのもの。段階は既に計算済みなので、表示を絞るだけ。

対象：`src/app/relocation/timing/page.tsx` のカレンダーヒートマップ（625 行あたり）

```
三盤吉 / 吉2盤・凶なし / 吉1盤・凶なし / 凶なし（平） /
軽い凶のみ / 五大凶殺あり / 天中殺
```

**注意：「天赦日・一粒万倍日」は軸が違う。**
上の 7 つは方位の吉凶（`DayTier` + `blocked`）だが、天赦日・一粒万倍日は**日の暦注**で、
方位とは独立に決まる。同じ選択肢に混ぜると「S かつ天赦日」が表現できない。
**別トグル（AND で重ねる）**にすること。

**受け入れ条件**

- 段階を複数選べる。既定は全選択（＝今と同じ見た目）
- 暦注トグルは段階フィルターと AND で効く
- 絞った結果 0 件の日は「該当なし」と分かる（空白と区別できる）
- 判定・段階の割り当ては変えない

</details>

---

### C. 物件検索に吉凶の分布を出す — **完了（#155）**

`src/components/relocation/DirectionTierOverview.tsx`。**提案どおりの積み上げ棒には
なっていない。**段階は方位ごとに 1 つしか出ないので積み上がらないことが実装中に
分かり、棒 1 本＝1 方位（長さ＝件数、色＝段階）に変わっている。判断としては正しい。

この作業中に「S と A が色だけでは見分けられない（ΔE 5.4）」が見つかり、
#156 の配色の組み替え、さらに #165・#168・#169 のダーク配色の修正へ繋がった。

<details>
<summary>着手前の記述（残す）</summary>

「物件を方位で探す」で全体像が掴めない、という指摘。

**提案：方位別（8 方位）× 段階の積み上げ棒。**
「南東に S が 12 件」が一目で分かる形。単純な段階別ヒストグラムより、
この画面の目的（どっちへ動くか決める）に合う。

- 色は `utils/tierDisplay.TIER_FILL` を使う。**新しい色表を作らない**
- 棒をクリックしたらその方位＋段階で一覧を絞れると尚よい

触るファイル：`src/app/relocation/arbitrage/page.tsx` ＋ 新規コンポーネント 1 つ

> グラフを描く前に `dataviz` スキルを読むこと。

</details>

---

### D. 暦カレンダーの重複を解く

いま 2 つの実装がある。

| 実装                                          | 使われている場所                                    |
| --------------------------------------------- | --------------------------------------------------- |
| `widgets/CosmicCalendar.tsx`                  | `/calendar`、ホーム（`SolarTimeClock` から 2 箇所） |
| `relocation/timing/page.tsx` の独自カレンダー | 引越しの日取りを選ぶ                                |

**提案：役割で割る。**

- **ホーム** — 「今日はどうか」だけ。月カレンダーは置かず、今日の詳細と `/calendar` への導線
- **`/calendar`** — 暦そのものを見るページ。`CosmicCalendar` はここが本籍
- **引越しの日取り** — 「いつ動くか」を決めるページ。方位 × 日のヒートマップが主役で、暦の詳細は `/calendar` に譲る

同じ月カレンダーが 3 か所にあると、どれが正なのか分からなくなる。
`docs/site-spec.md` の「同じことを 2 か所に書かない」と同じ話。

**この項目は見た目が変わる。**着手前に相談すること。

---

## 2. 相談してから着手するもの

判定の見え方が変わる。`CLAUDE.md` の「まず相談する」に当たる。

### E. 距離を評価に入れる

**指摘は正しい。**現状 `scoreProximity` は「近いほど高得点」という**通勤利便性の指標**であって、
方位の作用の強弱とは無関係。気学的な距離の扱いはどこにも入っていない。

思想の断片は 1 か所だけある。`lib/areaContent.ts` の

```ts
/** 近すぎる相手は方位が定まらないので除く。同一市内の区など。 */
const MIN_KM = 5;
```

**決めるべきこと。**

1. 距離で作用の強弱を変えるのか、変えないのか（流派差がある）
2. 変えるなら、しきい値と重みをどこに置くか（`utils/` に 1 か所）
3. 「近すぎて方位が定まらない」帯を判定にも入れるか（今は表示から除くだけ）

**決まるまで実装しない。**しきい値は九星気学の判定そのものなので、
`CLAUDE.md` の「数値・しきい値・条件式に触らない」に当たる。

---

### F. 「引越し先を試算する」の入口を作る

使えていない、という指摘。原因は**最初に何を入れればいいか分からない**ことだと思われる。
開いた時点で、地図・多段ステップ・同行者・迂回ルートが同時に出る。

**提案：入口を 1 段浅くする。**

- 「出発地」と「時期」だけで 1 候補を出す最短経路を作る
- 多段・同行者・迂回は、その後に「足す」形にする
- 今の全機能は残す（消さない）

**この項目も見た目が変わる。**着手前に相談すること。

---

## 3. 九星気学についての整理（実装ではなく前提の共有）

「東へ凶方位で移動 → 75 日 → 吉方位へ移動」で、最初の凶は相殺されるのか、という問い。

**流派で言うことが違う領域なので、サイトとして断定的に書かない。**
確実に言えるのは幾何のほうだけ。

### 第 2 移動が短いと、元の起点から見た方位は変わらない

京都を起点に計算した実測値。

```
東  30km → 南東  10km : 元の起点から E   ← まだ東の扇形の中
東  80km → 南東  30km : 元の起点から E   ← 同上
東 200km → 南東  80km : 元の起点から E   ← 同上
東  80km → 南東  80km : 元の起点から SE
東  10km → 南東  10km : 元の起点から SE
```

目安として、**第 2 移動が第 1 移動の半分ほどないと扇形を出ない**（45 度ずらす場合）。

**2026-08-27 追記 — 「1.5 倍程度」と書いてあったのは誤り。**上の実例は
どれも正しいが、そこから読み取った目安の数字が実測と合っていなかった。
判定エンジンの方位計算で閾値そのものを走査すると、東へ 100km 動いた
あと南東へ動く場合、**53km（0.53 倍）**で扇形を出る。上の実例が
0.40 倍で「出ない」・1.00 倍で「出る」なのと矛盾しない。1.5 倍は
その区間の外側にあり、必要な距離を 3 倍近く過大に見せていた。

必要な比は 2 回目の向きで変わる（1 回目に東へ 100km の場合）。

| 2 回目の向き | 出るのに要る距離 |
|---|---|
| 南東（45 度ずらす） | 53km |
| 南南東 | 33km |
| 南（直角） | 27km |
| 東（同じ向き） | **どれだけ進んでも出ない** |

扇形の幅でも変わる。四正（東西南北）は 30 度なので直角に 27%、
四隅（北東など）は 60 度なので 58% 要る。**四隅のほうが倍のスケールが
要る。**この実測は記事
`content/blog/honmeisatsu-year-board-next-move.md` に載せた。

75 日で基点が移る規則（実装済み）は「**次の移動をどこから測るか**」を決めるだけで、
「元の起点から見てどこにいるか」は別の話。ここを混同すると、
「吉方位へ動いたはずが、元の起点から見るとまだ凶方位の中」が起こる。

### 画面に出すとしたら

**提案：シミュレータで「元の起点から見た現在地の方位」を併記する。**
基点は移っても、元の起点からの方位は消えないという事実だけを示す。
相殺されるかどうかの解釈には踏み込まない。

これなら流派の判断をサイトが下さずに済み、利用者は自分の流派で解釈できる。

---

## 4. 着手順の提案

| 順  | 項目                                | 理由                 |
| --- | ----------------------------------- | -------------------- |
| 1   | ~~A. 俯瞰の無言フォールバック~~     | **完了（#147）**     |
| 2   | ~~B. ヒートマップの段階フィルター~~ | **完了（#154）**     |
| 3   | ~~C. 吉凶の分布グラフ~~             | **完了（#155）**     |
| 4   | D. 暦カレンダーの重複               | 要相談。範囲が広い   |
| 5   | E. 距離の評価                       | 要相談。判定そのもの |
| 6   | F. 試算の入口                       | 要相談。範囲が広い   |

lint 警告の削減（`CLAUDE.md` の 4 節）は、これらと並行して進めてよい。
触るファイルが重ならないよう、着手前に宣言すること。

---

## 5. lint の引き継ぎ — ローカル → クラウド

**645 → 377。**`SolarTimeClock.tsx` は 64 → 39 まで進み、その後の整理を経て
残っていた 13 件も解消した。対象ファイル単体の eslint は **0 件**。

### 済んだこと（ローカル）

| PR   | 中身                                                                        |
| ---- | --------------------------------------------------------------------------- |
| #211 | eslint の除外を `.gitignore` に合わせた。**数え方の前提**なので先に読むこと |
| #213 | `SolarTimeClock` の未使用 21 件                                             |
| #215 | 同 `catch` 4 件（`catch (e: any)` はこれで全社的にゼロ）                    |

### `SolarTimeClock.tsx` — 完了

```
0  eslint warnings
```

`solarData` / `NBAData` など応答系は、型を切る前にモデル化が要る。
**#212 で `api/nba` の応答は既にモデル化されている**ので、そこから引ける分がある。

### 判断が要る 2 つ（消さないこと）

`no-unused-vars` の 2 件は、**機能が初期値のまま止まっている**しるし。
lint としては消せるが、消すと機能を畳むことになる。

|                    | 実態                                                                         |
| ------------------ | ---------------------------------------------------------------------------- |
| `setMapProperties` | `mapProperties` は地図に渡しているが **setter を呼んでいない**。地図が常に空 |
| `setPressureDrop`  | `pressureDrop` は生体指標の計算に入るが **常に 0**                           |

### このファイルを触るときの注意

**字面での一括置換をしないこと。**実際に 2 回失敗した。

- `const dirs: Direction[] = [...]` は **8 か所**ある。1 つだけ未使用なのに、
  文字列置換で使っている側を消して `tsc` が 6 件のエラーを出した
- `const [a, setA] = useState()` は **警告が 2 件出る**。行番号で消すと
  同じ行を二重に削除し、隣の state（`showHowItWorks`）まで持っていった

eslint が指した**行番号**を使い、**同じ行を指す警告は畳んでから**消すこと。
どちらの事故も `npx tsc --noEmit` が止めた。**消したら必ず tsc を通す。**

### 消すと連鎖するもの（#213 で実際に出た）

書くだけの state を消すと、それを書くためだけの処理も不要になる。

- `latestTweet` … **30 秒ごとに `/api/agent/latest-log` を叩いて捨てていた**。
  取得と `setInterval` ごと外した
- `scrolled` … スクロールのたびに state を書いて再描画していた。リスナーごと外した
- `handleSentinelAction` … 呼ぶ場所が無く、この関数だけが出せる通知バナーも
  出ようがなかった。state と表示も一緒に外した

**「未使用の変数」の裏に、動いているのに何もしていない処理がある。**
消す前に、その値を書いているのが誰かまで追うこと。

### 変わっていない注意

- **`SolarTimeTable` の `nbaData` prop は呼び出し側が一度も渡していない。**
  全参照が `?.` 経由なので常に "N/A"。実質デッドだが、消す前に一度相談すること
- 重い依存は #177・#178 で `next/dynamic` に移した（KaTeX・recharts）。
  **静的 import に戻すと、ホームの表示がまた重くなる**
- 宇宙天気は #179 で `/api/space-weather` 経由になった。`fetchSpaceWeather` を
  直接呼ぶ形に戻さないこと（`next: { revalidate }` はブラウザでは効かない）

`utils/ephemerisEngine.ts`（16 件）と `utils/nbaEngine.ts`（12 件）は**判定の中核**。
型だけの変更でも、しきい値や条件式に手が滑ると影響が大きい。

---

## 6. lint とは別に、判断待ちで止めているもの

lint の作業中に見つけたが、画面の作りや流派の判断が要るので手を付けていない。

> **2026-08-27 追記 — この節の 2 項目は、どちらも既に解消済みだった。**
> 実装を確かめずに残っていた記述で、読んだ人に「まだ壊れている」と
> 思わせる。以下、確認した内容を残して取り消し線の代わりとする。

### ~~月命殺の点数が天中殺方位より低い~~（2026-09-07 に判断待ち → #1063・#1064 で解消）

**決着。**サイトの決め事は「評価は 1 系統だけ」（#698・#712）なので、
`noiseSeverity` の側に揃えた。月命殺・月命的殺は二次凶の 40 点。
`getStatusScore`・`api/municipalities-wealth`・`utils/arbitrageAstro` の
3 つの写しを `getStatusScore` 1 つにした。後の 2 つは **吉方位
（OPTIMAL_REGULAR）が写しに無く 50 に落ちていた**（平穏 80 より低い）
のも同時に直った。流派として月命殺を本命殺並みに見るなら
`FIVE_FATAL_NOISES` に足す。別の表を持たない。以下は当時の記録。

`lib/scoreTier.getStatusScore`（総合スコアの合成に使う 0〜100 の点）と
`api/municipalities-wealth` の同じ表は、凶を次のように点にしている。

| 状態 | 点 | 段階 |
|---|---|---|
| NOISE_GOU / ANKEN / HA | 10 | 大凶 |
| NOISE_HONMEI / TEKI / **GETSUMEI / GETSUTEKI** | 20 | 大凶 |
| NOISE_VOID / NODE | 40 | 警告 |

一方 `utils/noiseSeverity` は「順序と集合はこのファイルから引く。ここを
変えれば全部が揃って変わる」と宣言していて（#698・#712 で評価を 1 系統に
した置き場）、**月命殺・月命的殺は五大凶殺に入れず、天中殺方位と同じ
二次凶**に置いている。重さの順序も 天中殺方位 → 月命殺 → 月命的殺 →
羅睺計都 で、天中殺方位のほうが上。

つまり scoreTier は 月命殺 を本命殺と同じ「大凶」に、天中殺方位 を
「警告」にしていて、noiseSeverity の順序と逆。同じ方位が総合スコアの
升目では「大凶」、ラベルの畳み込みでは天中殺方位が先勝ち、という
食い違いが起きうる。

**直すなら** 月命殺・月命的殺を 40（警告）に下げて noiseSeverity の
集合に揃える（五大凶殺 = 10 / 20、二次凶 = 40）。総合スコアの升目の色が
月命殺の方位で 赤 → 橙 に変わる。流派によっては月命殺を本命殺並みに
見るので、ここは利用者の判断。`scoreTier.ts` 自身が「値は判定の
見え方を決める数字なので触らない」と書いてあるため、手を付けていない。
直すときは 3 節の手順（旧実装をテストに写して、戻すと落ちること）で。

### ~~見た目のコミットが機能を落としていた~~（#201 で発見 → 復旧済み）

`39d4bed`（2026-07-26 の見た目の変更）が物件スキャナーから
`<MetaphysicalConfigBar>` を JSX ごと落としていた件。

**2026-08-27 に確認したところ、既に戻っている。**
`src/app/relocation/arbitrage/page.tsx` の 13 行目で import され、
2484 行目で描画されている。2477 行目には `/houi` の
「設定で切り替えられます」という文言との対応もコメントで書いてある。
文章と実装の食い違いは解消済み。

### ~~運営者の生年月日が既定値のまま残っているファイル~~（解消済み）

挙がっていた 4 ファイルを 2026-08-27 に 1 つずつ確認した。
**運営者の生年月日は 4 つとも残っていない。**

| ファイル | いまの扱い |
|---|---|
| `api/nba/route.ts` | `let birthDateStr: string \| null = clientBirthDate \|\| null`。無ければ `personalBaziData` を作らない |
| `api/relocation/history/route.ts` | `let birthDate: Date \| null = null` を置き、`if (!birthDate)` で早期に返す |
| `nba/TenChiJinEvaluation.tsx` | 既定は空文字。`if (!birthDate)` で**指数を出さず**「生年月日が未入力です」を描く（397 行） |
| `relocation/timing/page.tsx` | `ls("arb_birthDate") \|\| ""`。`canScan` が false になり走査に入らない |

リポジトリ全体を `19[5-9][0-9]-[0-1][0-9]-[0-3][0-9]` で探しても、
残るのは入力例（「例：1965-05-15」「1990-01-01」）と案内文だけだった。

**残っている注意点は 1 つ。**`TenChiJinEvaluation` の `parseSafeDate` は
第 2 引数の既定が `new Date()` なので、**呼ぶ側がガードを外すと
「今日生まれ」で命式を組んでしまう**（#205 と同じ形）。397 行のガードが
それを止めている。ここを触るときは必ずガードごと見ること。

### 出生地が未入力のまま控え（プリセット）に残すと、東京か現住地が「出生地」になる（2026-09-08 に発見 → **2026-09-11 に解消**）

> **決着（2026-09-11）。**利用者の判断:「出生地未入力の場合、天体ラインを
> 出さないのがわかりやすい」。下の 3 段の順に 5 本で直した。
>
> 1. 読む側が `undefined` に耐える … #1188（3 か所）＋ #1189（tsc が出した
>    4 か所目 `ProfileForm`。grep では取りこぼしていた）
> 2. `ProfilePreset.birthLat/birthLon` を任意に … #1190。`isProfilePreset` と
>    API の zod は「両方あるか、両方無いか」。旧形式（wealth_presets）で
>    出生地が読めない控えは、以前は控えごと捨てていたのを、現住地と日付を
>    残す形にした
> 3. 書く側が埋めるのをやめる … #1191（ホームの簡易プロフィール＝東京駅、
>    設定バー＝現住地。案内文を「出生地が未入力のあいだは天体ラインの加点を
>    付けずに判定します」に）＋ #1202（ホームのプロフィール設定。
>    `birthPlaceOwned` を渡して初期値の東京駅を書かない）
>
> 見張りは `presetWritersNoBirthPlaceFallback.test.ts`（`??` 埋めと省略記法を
> AST で見る）と `profilePresetOptionalBirthPlace.test.ts`。API 側は元から
> `hasBirthLocation` で加点を止めるので、画面が埋めなければ加点は付かない。
> 以下は発見時の記述のまま残す。

`ProfilePreset` は `birthLat` / `birthLon` を**必須の数値**にしている
（`lib/profilePresetSync` の `isProfilePreset` も数値でなければ通さない）。
出生地は任意の項目なのに、控えに入れる時点で必ず何かの座標に
なる。**どこで保存したかで埋まる値が違う。**

| 保存する場所 | 出生地が空のとき何を書くか |
|---|---|
| ホームの簡易プロフィール（`home/QuickProfileBar`） | **東京駅**（`FALLBACK_LAT/LON`。画面にも「未入力の項目は東京で計算します」と出る） |
| 設定バー（`layout/MetaphysicalConfigBar` の `savePresetFromConfig`） | **現住地**（`config.birthLat ?? config.baseLat`） |
| ホームのプロフィール設定（`PersonalProfileConfig`） | `SolarTimeClock` の state の初期値＝**東京駅** |
| 移住先の比較（`relocation/wealth`） | 空なら **alert で止める**（書かない） |

控えを呼び出すと（`handlePick` / `applyPreset` / 各頁の `handleLoadPreset`）
その座標が `birth_lat` として設定とクラウドに書かれる。以後、物件検索と
移住先の比較で**天体ライン（太陽・金星・木星）の加点が、東京または
現住地で生まれた人として付く。**API 側は出生地が無ければ加点を付けない
作りになっている（`hasBirthLocation`）ので、「分からない」を「分からない」の
まま運べれば、加点は付かないのが正しい答え。偏角を「取れなければ 0」に
したのと同じ考え方（CLAUDE.md 3 節）。

#1114 で塞いだ「出発地が東京駅として保存される」と同じ型だが、
こちらは**型が必須にしているせいで、書く側が何かを埋めざるを得ない。**
直すなら型から。読む側が先で、書く側は最後（順を逆にすると
`String(preset.birthLat)` が `"undefined"` を出す）。

1. 読む側が `undefined` に耐えるようにする（`wealth/page` の
   `String(preset.birthLat)`、`account/AccountPanel` の `formatCoords`、
   `home/QuickProfileBar` の `handlePick`）。挙動は変わらない
2. `ProfilePreset.birthLat/birthLon` を任意にし、`isProfilePreset` と
   `api/profile-presets` の検証を「両方あるか、両方無いか」にする
3. 書く側が埋めるのをやめる（`QuickProfileBar`、`MetaphysicalConfigBar`）。
   `PersonalProfileConfig` は `SolarTimeClock` の `birthPlaceOwned`（#1100）
   を見ないと「未入力」と「東京」を区別できないので、ここだけ別途

8 ファイルほどに散るので 3 つの PR に割る。**着手前に、ホームの
「未入力は東京で計算します」の案内をどうするか**（計算自体は東京で
続けるのか、出生地が無い間は天体ラインを出さないのか）を決める必要が
ある。ここが利用者の判断。

---

## 7. 検索に載らない件（インデックス）

Search Console の実測（2026-08-13 時点）。

```
インデックス済み          3 ページ
未登録                   49 ページ
  クロール済み-未登録     36   ← いちばん大きい塊
  見つかりません（404）    9
  robots.txt でブロック    1
  403 でブロック           1
  noindex                 1
  重複・正規未選択          1
表示回数（3 か月）         1 回（クリック 0）
サイトマップ              sitemap.xml / sitemap.rss とも「取得できませんでした」
                         送信 2025-09-07・最終読み込み 2025-09-11
```

### コード側で潰した疑い（すべて原因ではない）

| 疑い                             | 確かめ方と結果                                                                        |
| -------------------------------- | ------------------------------------------------------------------------------------- |
| サイトマップの中身               | 正常。index → `sitemap-0.xml`、**813 URL**、130KB                                     |
| ビルドで生成されない             | 生成される。`.dockerignore` も `public/sitemap*.xml` を除外していない                 |
| イメージに入らない               | Dockerfile が `COPY --from=builder /app/public ./public`。standalone の公式手順どおり |
| `next.config` の転送・書き換え   | `/sitemap.xml` に触るものは無い（`www` → apex の 301 のみ）                           |
| `robots.txt` で塞いでいる        | 塞いでいるのは `/admin`・`/api`・`/relocation/history` だけ                           |
| canonical が無い                 | `/admin/metrics`（意図的に noindex）以外**全ページにある**                            |
| 量産ページの題名が重複           | 年・星ごとに固有（`2026年 一白水星の吉方位と凶方位`）                                 |
| サイトマップに認証・管理系が混入 | 0 件。813 件すべて公開ページ                                                          |

**ブラウザで `https://cloud-palette.com/sitemap.xml` を開くと正常に XML が返る**ことを
利用者が確認済み（2026-08-13）。**ファイルは本番に存在し、200 で配信されている。**

### 訂正 — #258 の説明は誤りだった

#258「サイトマップに認証を掛けていたのをやめる」の本文に

> `/sitemap.xml` を取りに来ただけで updateSession が走り、1 回ごとに
> Supabase の getUser() へネットワーク往復が入る

と書いたが、**これは誤り。**`utils/supabase/middleware.ts` には既に番人がある。

```ts
// On public routes without auth cookies, skip remote auth network call
if (!requiresAuthentication && !isLoginPage && !hasAuthCookie) {
  return supabaseResponse;
}
```

クローラは Cookie を持たず `/sitemap.xml` は保護対象でもないので、**往復は起きていない。**
#258 の変更自体（公開ファイルに middleware を通さない）は妥当だが、**取得失敗の
原因の説明にはなっていない。**同じ勘違いを繰り返さないため、番人の存在をここに残す。

### 残っている有力な原因 — コールドスタート

```
--min-instances=0   ← 待機インスタンスなし。途切れると毎回起動から
--memory=512Mi
--cpu=1
```

Google が取りに来たときにコンテナが停止していれば**起動から始まる**。Next.js
standalone ＋ Prisma を 512Mi で立ち上げるので初回応答が遅く、取得が失敗と
記録されうる。クロール全体が伸びないこと（クロール済み-未登録 36 件）とも整合する。

**`--min-instances=1` は課金が発生する**ので、`CLAUDE.md` 6 節の「戻せないもの」に
当たる。**利用者の判断を待つ。**費用をかけない側の手当て（Prisma の初期化を遅らせる、
イメージを小さくする）なら自走してよい。

### 404 の 9 件について

削除済みの 10 ページ（`/dashboard` `/metaphysical` `/trends` `/rentals` `/visualizer`
`/x-viewer` `/research` `/extract` `/agent-log` `/ceremonial-sample`）とほぼ一致する。
**404 を返すのが正しい挙動**で、Google 側で自然に消える。転送すると別ページへの
誘導になるので**触らない**。

### `/sitemap.rss` は存在しない

`next-sitemap` は RSS を作らない。登録が残っていると永久に失敗し続ける。
利用者が Search Console から削除済み（2026-08-13）。**コードでは直せない。**

---

## 8. 管理画面の運用経費

「必要になっている経費も表示したい」への対応。**3 段構えで、利用者の指示は「全部」。**

| 段  | 中身                                   | 状態                   | 手作業になる部分                            |
| --- | -------------------------------------- | ---------------------- | ------------------------------------------- |
| 1/3 | 固定費（手で維持する表）               | **完了（#257）**       | なし                                        |
| 2/3 | 外部 API の従量（呼び出し回数 × 単価） | **完了**                 | 本番 DDL・単価とも適用済み                |
| 3/3 | GCP の請求実額                         | **本番基盤まで設定済み** | コンソールで Standard export を有効化する |

### 金額の扱い（#257 で決めたこと）

**推測の額を入れない。**分からないものは `null`（未設定）にして画面にもそう出す。
もっともらしい額を置くと、あとから見た人がそれを実額だと思い込む。#231 で
存在しない項目を読んで NaN を出していたのと同じ筋。

- **一部だけ埋まった状態では合計を出さない。**足すと実際より小さい額が「合計」として
  出る。経費は割に合うかを判断する数字なので、小さく出るのが一番まずい
- **0 円と未設定は別物。**無料枠に収まって 0 円は確かめた事実、未設定は調べていない
- 単価は分母が 0 なら出さない（登録者 0 人で Infinity や NaN を出さない）

### 埋めてもらう必要があるもの

`src/lib/operatingCosts.ts` の `monthlyYen`。**5 件とも未設定のまま。**

```
cloud_run / supabase / domain / artifact_registry / external_api
```

---

## 9. お気に入り（#241・#242）

DDL 適用済み・稼働中。**ログイン中はクラウド、未ログインは端末**に保存し、
ログインしたときに端末のぶんを寄せる。クラウドが落ちていても操作は成立する。

### 残っていること

- ~~★ を押せるのは物件の詳細パネルだけ。~~ **カード・表・TOP5 にも追加済み。**
  星だけを押したときは詳細を開かず、お気に入りだけを切り替える
- **掲載終了の物件の出し方。**`property_id` は外部キーにしていないので id は残るが、
  引けなかったものを「掲載終了」として出す処理はまだ無い
- **★ ボタンの実データでの見え方が未確認。**この環境には賃貸データが無く、詳細
  パネルまで辿り着けない

---

## 10. 頁の横幅 — 決着済み

**全画面 1700px（地図を横に並べる `arbitrage`・`wealth` だけ 2560px）。**
記事も含めて揃えた（#238・#239・#250〜#254）。決めごとは `CLAUDE.md` 3 節。

例外は 3 つだけ。

```
/login                        max-w-md    入力欄が主役。伸ばしても何も買わない
/relocation/market の準備中     max-w-3xl   データが無いときの案内カード
/relocation/timing の 1 段落    max-w-md    段落側で行長を守っている箇所
```

**記事だけ狭くする例外を戻さないこと。**読みにくさが出たら器ではなく段落側で
対処する（`max-w-[70ch]`、目次を横に置いて 2 段、など）。

---

## 11. 表示速度 — 主犯は CSS ではなく JavaScript（実測 2026-08-18）

PageSpeed Insights の点数が低い件。**最初「CSS の 184KB を削るのが一番効く」と
書いたが、実測したら効かなかった。**ここに測定値ごと残す。

### 測り方

`npm run build` の standalone をローカルで起動し、Lighthouse 12 をモバイル・
既定のスロットリングで掛けた。本番ではなくローカルなので絶対値は比べない。
**同じ場所で前後を比べる**（lint の総数と同じ注意）。

```bash
cd .next/standalone && PORT=3066 node server.js
CHROME_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome \
  npx lighthouse@12 http://127.0.0.1:3066/ --only-categories=performance \
  --form-factor=mobile --screenEmulation.mobile --output=json
```

`public/` と `.next/static` を standalone 配下に写し忘れると**全資材が 404 に
なったまま測れてしまう。**その状態の点数は 100 になる。リクエスト数
（正常なら 123 件）を必ず確認すること。1 度これで「100 になった」と誤読した。

### 結果

| | 2 ウェイト（現行） | 1 ウェイト（実験） |
|---|---|---|
| Performance | **43** | **40** |
| FCP | 3.6 s | 3.3 s |
| LCP | 7.1 s | 9.0 s |
| TBT | 1,020 ms | 1,530 ms |
| 初期 CSS 転送（gzip） | 96 KB | 64 KB |
| 描画を止めた合計 | 1,746 ms | 1,446 ms |
| scriptEvaluation | 2,885 ms | 3,008 ms |

**CSS は狙いどおり 32KB 減り、描画を止める時間も 300ms 減ったのに、点数は
上がらない。**43 → 40 は測定のばらつきの範囲。Lighthouse 自身も
`render-blocking-resources` を **`Est savings of 0 ms`** と評価している。
この行を読まずに CSS を主犯だと書いていた。

**フォントのウェイト削減は採用しない。**効果が無いうえ、セリフ体テーマ
（`fontTheme === "serif"`）の本文が 400 を使う。`layout.tsx` のコメントに
書いてあるとおり。

### 主犯

メインスレッドの内訳（2 ウェイト版）。

```
scriptEvaluation   2,885ms   ← 4 割
styleLayout        1,935ms
other              1,348ms
scriptParseCompile   391ms
```

`bootup-time` の筆頭は chunk `3794-…js` で **2,540ms（うち scripting 2,000ms）**。
中身を見たら **React と Next のランタイム本体**だった。

**DOM は 493 要素しかない。**要素数ではなく、`src/app/page.tsx` が丸ごと
`"use client"` で `SolarTimeClock`（8,000 行）をハイドレートしているコスト。
`dynamic(..., { ssr: false })` で分割はされているが、**読み込んだ後は必ず
実行される**ので初回表示の待ち時間にそのまま乗る。

### 何が読まれているか

ホームで読まれる JS は **gzip で 386KB・40 本。**時刻順に見ると 2 波ある。

```
191ms   webpack / 4bd1b696(63KB=React) / 3794(52KB) / app/page(11KB)   ← 初期
858ms   44530001(12KB) 5536(50KB) 428ccf76(100KB) 481c1c7c(20KB)
        5852(52KB)                                                     ← 時計の依存
```

2 波目は `SolarTimeClock` の `dynamic` import が引いてくる依存。中身を見ると

| chunk | gzip | 中身 |
|---|---|---|
| `428ccf76` | **100KB** | **lunar-javascript**（干支・節気・八卦。判定の土台） |
| `5852` | 52KB | — |
| `5536` | 50KB | **Supabase**（`GlobalSidebar` が遅延読みしている当のもの） |
| `481c1c7c` | 20KB | astronomy-engine |
| `44530001` | 12KB | Supabase の続き |

**単独で一番重いのは lunar-javascript の 100KB。**判定に要るので消せないが、
初回表示の前に要るものではない。

### 上の「Supabase が時計の依存に混ざっている」は誤りだった（訂正）

一度そう書いたが、**静的 import を全部辿ったら Supabase に到達しない。**
入口（57 ファイル）からも `SolarTimeClock`（47 ファイル）からも該当なし。

実際は `GlobalSidebar` 自身。`loadSupabase()` を **mount の `useEffect` で
呼んでいる**（同ファイル 94 行）。ログイン状態を出すためなので当然の作り。

**遅延読みは意図どおり効いている。**共有バンドルには入っておらず、
コメントが心配していた「`/houi` の記事ページでも 60KB 配る」状態には
なっていない。**時間として遅れていないだけ**で、それを「効いていない」と
読み違えた。

### 実体は入口の束ね読みだった（#392 で解消）

`src/app/page.tsx` は時計を**まとめ役の入口経由**で読んでいた。

```ts
import("@/domains/metaphysical").then((mod) => mod.SolarTimeClock)
```

`@/domains/metaphysical/index.ts` は **8 つの部品を再輸出する入口**で、
1 つだけ取り出しても**束ねられた 8 つ全部が同じチャンクに入る。**
ホームが描くのは時計だけなので、残り 7 つは一度も使われない。

静的 import を辿ると、入口経由 57 ファイル・部品直 47 ファイル。
余分な 10 本はソース計 116,826 バイト。

```
SolarTimeTable(34KB)  BioMagneticDashboard(23KB)  TacticalMagneticMap(19KB)
kmlExport(14KB)  VolumetricBioMap(9KB)  MagneticSpatialHUD(7KB)
TenchusatsuVisualizer(5KB)  tenchusatsu(3KB)  KigakuBoard(3KB)  index.ts
```

部品を直に読むよう 1 行変えた結果。

| | 入口経由 | 部品直 |
|---|---|---|
| **JS 転送（gzip）** | **386,058 B** | **142,951 B（-63%）** |
| **未使用 JS** | **70,567 B** | **0 B** |
| Performance | 43 | 48 / 51 / 54（3 回） |
| LCP | 7.1 s | 5.3〜5.7 s |
| Speed Index | 5.3 s | 3.5〜3.6 s |
| TBT | 1,020 ms | 800〜1,120 ms |
| TTI | 8.0 s | 6.3 s |
| scriptEvaluation | 2,885 ms | 2,650 ms |

**JS 転送の数字は 3 回とも同じ 142,951 バイト**（ビルドで決まるので揺れない）。
点数のほうは ±3 揺れる。**元の 43 は 1 回しか測っていない**ので、点数だけで
判断せず転送量のほうを根拠にすること。

**「入口は他の頁が使うので残す」と書いたが、誤りだった（訂正）。**

`src/domains/` の入口は 4 つある。

```
src/domains/finance/index.ts        2 件を再輸出
src/domains/launcher/index.ts       6 件
src/domains/metaphysical/index.ts   8 件
src/domains/realestate/index.ts     3 件
```

**#392 のあと、この 4 つを読むコードはリポジトリ内に 1 行も無い。**
唯一の一致は `src/app/page.tsx` のコメント自身。scripts と設定ファイルまで
含めて探した。`metaphysical` は #392 で最後の読み手を失い、他の 3 つは
元から読み手が居ない。

害は出ていない（誰も読まないのでどのチャンクにも入らない）。ただし
**扱いは要相談**（CLAUDE.md 4 節「ファイルを丸ごと消す」）。

- 4 つとも消す — 安全だが、領域ごとに入口を置く設計の意図ごと捨てる
- 残して、使わない理由をコメントに書く — **こちらを勧める。**入口を
  消しても速度は 1 バイトも変わらず（既に誰も読んでいない）、逆に
  「なぜ入口があるのに使わないのか」が分からなくなって、将来また
  束ね読みに戻る事故が起きやすい

`VolumetricBioMap` は**リポジトリ全体でどこからも使われていない。**
→ **#628 で消した。**参照ゼロ（docs とテストのコメントでの言及のみ）を
確かめたうえでの判断。git revert で戻る。

### 次にやるとよいこと（未着手・要相談）

1. **時計を画面に入るまで動かさない。**`IntersectionObserver` で
   `SolarTimeClock` の mount を遅らせる。ホームの主役の出方が変わるので
   相談してから
2. **同じ束ね読みが他の頁にも無いか。**入口を作ってある領域は
   `src/domains/` 配下にいくつかある
3. `color-contrast` と `font-size` は accessibility 側（下記）

### accessibility で残っているもの

Lighthouse の A11y は 90。`button-name` は #390 で 0 件にした。残りは 2 つ。

- **`color-contrast`（score 0）** — `text-stone-400` の小さい字と
  `text-emerald-600` が主。配色の決めごとを変える話になる
- **`font-size`（score 0）** — 読める大きさの字が **57.95%** しかない。
  `text-[7px]` `text-[9px]` `text-[10px]` が原因

### 直したもの（#389）

`public/` のアイコン 3 枚が「中身は 1024×1024 の JPEG、拡張子だけ `.png`」の
**バイト単位で同一のファイル**だった（各 501,687 バイト）。`manifest.json` は
192×192 / 512×512 / 180×180 の `image/png` と宣言していたので**型も寸法も全部
うそ**。`favicon.ico` は存在せず、全ページで 404 が 1 件出ていた。

寸法ごとに作り直して **1,505,061 → 126,933 バイト（-92%）。**
`__tests__/appIcons.test.ts` が `manifest.json` の側から引いて PNG の IHDR と
突き合わせるので、拡張子だけ直した状態では通らない。

## 8. `finalVectors` の型が八方位しか埋めていないのに CENTER を約束している

`calculateVectorCollision` / `filterCollisionByMode` の戻り値は
`finalVectors: Record<Direction, VectorStatus>` を名乗っている。ところが
`Direction` には盤の中心（`CENTER`）が入っていて、**実装は八方位しか
埋めていない。**`finalVectors.CENTER` は実行時 `undefined` なのに、型は
`VectorStatus`（非 undefined）を返すと言っている。**型の嘘。**

#536 で判定の型を整理したとき、`Record<Exclude<Direction, "CENTER">, …>`
に直そうとしたら、`Direction` で回して添字を引いている呼び出し側が
一斉に落ちた。**落ちた場所はどれも、実行時に `undefined` を掴みうる
場所**なので、エラーのほうが正しい。

```
__tests__/ignoreDayLayer.test.ts(61)
src/app/api/relocation/export/route.ts(402)
src/app/api/relocation/history/route.ts(315)
src/app/relocation/simulator/page.tsx(694, 732, 1021, 1061)
src/components/SolarTimeClock.tsx(1350, 1697, 1707, 1717, 1727, 1737)
```

`SolarTimeClock.tsx` の 1697 以降は、**戻り値の形をその場に写した
ローカルの型**（`{ yearLayer: Partial<Record<Direction, string>>; … }`）
を引数に持っている。`VectorCollision` に寄せれば一緒に消える。

### なぜ #536 で直さなかったか

1 PR = 1〜3 ファイルに収まらない（6 ファイル）。型だけの整理と、
呼び出し側の `undefined` の扱いを決める作業は別物でもある。

### 直すときの順序

1. `ephemerisEngine` に `EightDirection = Exclude<Direction, "CENTER">`
   を出し、`finalVectors` をそれで型付ける
2. 落ちた呼び出し側を 1 つずつ見て、**`CENTER` を引いているのが
   意図なのか事故なのか**を決める。事故なら回す配列を八方位に絞る
3. `SolarTimeClock` のローカルの型を `VectorCollision` に置き換える

**器（`finalVectors`）に `CENTER: "SAFE"` を足して型に合わせてはいけない。**
実データに無い鍵が増え、JSON 書き出し（`api/relocation/export`）の中身が
変わる。今 `undefined` を掴んでいる箇所は `"SAFE"` を掴むようになるので、
**画面の答えも変わる。**直すのは型のほうであって、データではない。

### 決着 — #635〜#638 で段階的に直した（2026-08-26）

上の「直すときの順序」のとおり、器ではなく型を直した。データの鍵は
1 つも増減していない。

1. #635: `EightDirection` と `EIGHT_DIRECTIONS` を導入。
   `ALL_DIRECTIONS` を別名化（手書きの八方位配列は最終的に 6 個あった）
2. #636: 方位角→方位の根元（`getKigakuSector`）と route 2 つを八方位に。
   **広げていたのは受け側ではなく変換の戻り値**だった
3. #637: 画面側（simulator・SolarTimeClock・ConsultPanel）の器と
   ローカル型を八方位に。`trueDirection as Direction` の cast が外れた
4. #638: `VectorCollision.finalVectors` 本体を
   `Record<EightDirection, VectorStatus>` に切り替え。engine の
   `finalVectors: any` 2 件が 0 になり、tsc が字面で見つからない受け手
   （`AuspiciousDayParams.direction`・`EvaluationContext.targetDirection`）
   まで教えてくれた

`ScorecardDirection`（ScorecardPanel）は同じ型の双子だったので
`EightDirection` の別名にした。`__tests__/ignoreDayLayer.test.ts` は
`tendoDirection` を八方位に絞ったことで無修正のまま通っている。

## 9. `/api/v1` を呼ぶ画面がまるごと死んでいる（2,829 行）

`MarkdownViewerWidget` の `error` が未使用（`setError` は呼ばれているのに
**画面に出していない**）のを追ったら、その先が全部死んでいた。

### 何が起きているか

`src/app/api/v1/` は**存在しない。**ところが 7 つの widget がそこを
呼んでいる。

```
/api/v1/files/list        MarkdownViewerWidget
/api/v1/files/read        MarkdownViewerWidget
/api/v1/files/download    MarkdownViewerWidget
/api/v1/scraper/*         MediaScraperWidget
/api/v1/xtrends/search    XTrendsWidget
/api/v1/youtube/chat      YouTubeChat
/api/v1/youtube/extract   YouTubeExtractor
```

`MarkdownViewerWidget` は取得に失敗すると `setError` するが、`error` を
描いていないので、画面には **「ファイルがありません」** と出る。実際は
「API が無い」。**失敗が「空」に化けている。**

### 呼び出し元をたどると全部 1 か所に集まる

    DynamicCanvas.tsx ← **どこからも import されていない**
      ├ DataAnalyzerWidget
      ├ MarkdownViewerWidget
      ├ OmniPipelineWidget
      ├ XTrendsWidget
      ├ YouTubeChat
      └ YouTubeExtractor
    MediaScraperWidget ← どこからも使われていない

`useAgentStream`（hook）と `analyzerResult`（lib）もこの系統。
合計 **2,829 行**。

CLAUDE.md の `next-sitemap.config.js` に「すでに削除した 9 ページ
（/dashboard /visualizer /x-viewer /agent-log …）」と書いてある。
**この widget 群はその頁の残骸**だと思われる。頁は消したが、部品と
`/api/v1` を呼ぶ配線が残った。

### 決着 — #559 で 1 案（まとめて消す）を採った

`DynamicCanvas`・widget 群・`useAgentStream`・`analyzerResult` は
**#559 で削除済み**。2026-08-26 に再確認したところ、`src/` に
`/api/v1` を呼ぶコードは 0 件（`MarkdownViewerWidget` の
「失敗が空に化ける」問題も、部品ごと消えたので解消）。
この節は経緯の記録として残す。

`VolumetricBioMap`（8 節の下の方に既出）は #628 で消した。

---

## 12. 土用殺の切り出しを見送った理由（2026-08-25 に判断）

### 何が問題か

土用殺は年盤・月盤・日盤の**どの層にも出ない**。
`calculateVectorCollision` の最後で**最終だけを `NOISE_GOU` に上書き**する。
`NOISE_GOU` の呼び名はどこでも「五黄殺」なので、

    年盤 大吉 / 月盤 大吉 / 日盤 大吉  なのに  最終 五黄殺・段階 X

という日ができる。**理由の表示が間違っている。**実測で本命七赤・
子丑空亡の 400 日 × 8 方位のうち 8 通りが該当した（秋土用の北西・
春土用の南東など）。

### やったこと

#568 で `VectorCollision.doyouSatsuDirection` と
`DayVerdict.isDoyouSatsu` を足した。#569 で `SpotVerdict` が理由の
1 行を出す。**判定は変えていない。**

### `NOISE_DOYOU` として切り出すのを見送った

切り出すには、`NOISE_GOU` を**挙動として**見ている場所を全部
同時に直さないと判定が変わる。数えたら **15 ファイル**あった。

    MagneticMapInner（2 か所）      TenChiJinEvaluation
    DestinationMapPanel            ConsultPanel（4 か所）
    SolarTimeClock                 api/municipalities-wealth（点数の switch）
    api/rentals/arbitrage          api/relocation/history（2 か所）
    api/relocation/export          relocation/history/page
    relocation/simulator/page（4 か所）  lib/scoreTier
    utils/arbitrageAstro           utils/ephemerisEngine（3 か所）

**費用と効果が釣り合わない。**

  得られるもの … 地図の札が「五黄」→「土用」に変わる。年 60 日 × 1 方位
  払うもの     … 15 ファイルの分岐を書き換える。1 つ漏らすと判定が黙って変わる

判定そのものは**いまも正しい**（土用殺の日は X で候補に出ない）。
間違っているのは**語だけ**。語のために判定の分岐を 15 か所いじるのは
割に合わない。

### 代わりにやるとよいこと（安い順）

1. **`directionLabels` に `NOISE_DOYOU` の語だけ足す。**状態としては
   使わず、「土用殺だと分かっている呼び出し側」が鍵として使う。
   engine は触らない
2. **地図に `doyouSatsuDirection` を渡す。**`MagneticMapInner` の
   `layers` prop は 4 つの層しか受け取っていないので、そこに 1 つ足す。
   API の応答にも足す必要がある（2〜3 ファイル）
3. 物件検索の `maxAstroFactor` も同じ。`targetDetails.doyouPenalty < 0`
   の枝は既にあるが、`NOISE_GOU` の枝より**後ろ**にあるので永久に
   届かない。方位が一致するかを見て前に出す

**1 → 2 → 3 の順なら、どれも 1〜3 ファイルに収まる。**

---

## 13. `/houi/area/39205` の 404 を受け入れる（2026-08-25 に判断）

Search Console の 404 一覧（2026-08-24 書き出し）に
`/houi/area/39205`（高知県土佐清水市）が残っている。最終クロールは
2026-08-22。

`src/data/areaDirections.json` の**直近 8 版すべてに存在しない**。
掲載が閾値に満たなくなって落ちたまま、戻っていない。

#533 の引き継ぎは「**前回のファイルにあったもの**を残す」仕組みなので、
**それ以前に落ちた市区町村は戻らない。**39205 には引き継ぐ数字自体が
残っていないので、復活させるならデータ無しの状態で描くことになる。

### 受け入れる理由

- この頁は #379 で **noindex** にしてある。索引から失うものが無い
- 数字の無い相場の頁を出すのは、**404 より悪い**。「相場が分からない」
  ことを「相場のページがある」形で見せることになる
- 恒久的に直すなら「**一度公開した code の台帳**」を別に持って、
  落ちても枠だけ残す作りが要る。1,000 件規模の URL を永久に維持する
  約束になるので、noindex の頁のためにやることではない

### もし気が変わったら

台帳を持つより、**落ちた市区町村は都道府県の頁へ 301 で寄せる**ほうが
筋がよい。URL は生き、中身は必ずある。ただし都道府県の頁が要る。

---

## 13. 生存分析の「中央 0 日」は巡回の網羅漏れ（実測 2026-08-26）

/relocation/market の KPI「掲載の中央存続期間」が 0 日になっていた件。
本番に SELECT だけの調査 SQL を当てて切り分けた
（`prisma/sql/20260826_probe_survival_oneshot.sql`。db-apply-sql で実行）。

### 実測

- 全 985,042 行のうち **52.5 万件（53%）が一度しか観測されずに
  「7 日見かけない＝終了」扱い**
- **長崎は巡回が 8/14 から 12 日間停止**。全 3,571 件が偽の終了扱い
- **北海道は 8/24 に巡回しているのに 91% が終了扱い**。1 回の巡回で
  在庫 43,688 件のうち 3,795 件しか再訪できていない
- 毎日巡回の metro でも東京 62% / 神奈川 83% / 埼玉 68% が終了扱い
- 再掲載（URL 揺れ）の痕跡は標本 2 万件中 5.1% のみ。**主因ではない**

### 追記（2026-08-27）— 網羅漏れの原因が特定できた。予算ではなかった

「巡回の網羅漏れ」の中身を追ったら、**時間が足りないのではなく、
巡回が予算を使わずに終わっていた。**8/25 の run の実測:

    長崎   予算 50 分 → 1.2 分で「成功」（最終巡回は 8/14 のまま）
    北海道 予算 100 分 → 1.2 分。1 ページも取らずに「成功」
    石川   予算 50 分 → 8.4 分
    愛知   予算 100 分 → 16.4 分（前々日も 17.9 分。毎回同じ位置から）

原因は再開位置の引き継ぎ。CI は actions/cache で日をまたいで引き継ぐが、
**完了時にステートファイルを消していた**ため post 手順が
「Path Validation Error ... no cache is being saved」で保存を飛ばし、
古い再開位置がいつまでも残る。県は毎晩「途中から末尾まで」しか回らず、
その手前は二度と再訪されない。北海道はさらに、再開位置の市区町村が
一覧から消えていて全スキップ＝毎晩 8 秒の空振りだった。

**#648 で 3 スクレイパー全部（nifty・eheya・shamaison）を直した。**
消すのではなく空を書く／再開位置が一覧から外れたら先頭から／1 つも
回らずに終わったら「成功」と言わない。

**したがって「予算を増やす」は、少なくとも今は要らない。**予算は
余っていた。効果は #648 マージの翌朝の巡回から出る。

### まだ答えの出ていない問い — 市区町村一覧が細い

nifty の一覧は都道府県トップページの `_ct/` リンクから作っている。
8/25 の北海道は **10 件**しか取れていなかった（在庫 43,688 件）。
しかも再開位置にあった fukagawashi はその 10 件に無い＝日によって
中身が変わる。一覧が細いままなら、再開位置を直しても届かない
市区町村が残る。`prisma/sql/20260827_probe_crawl_coverage.sql` の D で
「DB が知っている市区町村の数」を測れば上限が分かる。

### 結論と対応（当初の記録）

主因は**巡回の網羅漏れ**。「metro は毎日・regional は 3 日おきに全件を
回る」という生存分析の前提（build_market_stats のコメント）が崩れている。
7 節の #47（夜間巡回が 6 時間の枠に収まらない）と同根。

- 分析側は正直にした: KPI は中央値が 1 日以下のとき「計測不能」を出し、
  生存曲線の副題に歪みの向きと理由を明記（市場の実態より速く落ちる）
- **巡回設計の変更（予算増・全県の再訪保証・resume の見直し）は
  #47 と一体で、利用者の判断が要る。**自走しない
- 巡回が直ったら、この節の実測を再取得して KPI の「計測不能」条件を
  外せるか確かめる

### 再測（2026-08-30。run 33290524812）

#648 の一巡修正と #667 の cron 分ずらしのあとに同じ probe を回した。

    一度きり観測      nifty 72.8% / eheya 67.1% / shamaison 3.7%
    最終巡回          46 都道府県が 8/24〜8/30 に回復
    長崎              **8/14 のまま。fresh_pct 0.0%（在庫 2,728 件）**

長崎だけ残った原因は 2 段の合わせ技（#713 に詳細）:

1. resume バグ（#648 修正済み）で 8/14 から空振り
2. 修正後の最初の予定日 8/28 の定時 run が GitHub の混雑で 7 時間遅れて
   8/29 02:09 に起動し、**起動時の UTC 日付で計画するため予定日が
   8/29 に化けて**、3 日周期の長崎が matrix から脱落した

対応（2026-08-30 実施）:

- #713: 定時起動の計画日付を `now - 12h` の日付に固定（遅延 16 時間 43 分
  まで cron の日のまま）。手動実行は従来どおり
- 6 時間 queued のまま固まっていた run 35 をキャンセル
- prefectures=nagasaki の救済実行を投入

次の再測で見るもの: 長崎の fresh_pct が立ち上がるか。nifty の一度きり
観測 72.8% は #647 時点の 53% より**悪化して見える**が、分母も観測期間も
違うので同列に比べない。巡回が安定して数日回ったあとの値で判断する。
市区町村一覧の細さ（D の再測: 北海道 126・埼玉 72・大阪 71 と、
DB が知る数はある。一覧の取得側の問題か引き続き確認）。

## 14. 頁の「同じ地の文」は description にも残っていた（2026-08-31）

#379 で市区町村 1,022 頁を索引から外した理由は「**地の文はどの URL でも
同一**、変わるのは地名と表の数字だけ」だった。#750 から本文は 1 頁ずつ
書き分けてきたが、**meta description は地名だけを差し替えた同じ 1 文を
全頁に配ったままだった**（#805 で解消）。

    ${area.full}を出発地としたとき、北・北東・東・…をまとめています。   ← 1,022 頁
    ${pref}の市区町村ごとの家賃相場と、県の中心から見た八方位…にしました。 ← 47 頁

検索結果に出るいちばん目立つ場所が、いちばん最後まで直っていなかった。

**教訓は「同じ地の文」を本文だけで探さないこと。**description・OGP・
JSON-LD・llms.txt のように、頁の外に出る文字列は本文と別に持っている
ことが多い。同じ日に llms.txt からは**地域ページが丸ごと抜けている**
のも見つかった（#806。載っていたのは機能の一覧・ガイド・記事だけで、
手で書いた 47 県が 1 つも出ていなかった）。

### 併せて分かったこと（同じ日の実測）

| 何                       | 実測                                                            | 対応                                      |
| ------------------------ | --------------------------------------------------------------- | ----------------------------------------- |
| 県ページの文章に家賃の額 | 47 県のうち 33 県。**19 県は既に表と食い違い**                  | #792・#793 で全部外し、検査で書けなくした |
| 県ページの空の方位       | **47 県で 151 方位**（八方位×47=376 の 4 割）が黙って消えていた | #800                                      |
| 市区町村 → 県の導線      | **0 本**（県 → 市区町村は降りられる）                           | #799                                      |
| 県ページのパンくず       | 画面・JSON-LD ともに無し                                        | #802                                      |
| 文章の方位の取り違え     | 県 107 件・市区町村 226 件を計算に当てて **0 件**               | #795・#796 で検査として固定               |

方位の取り違えが 0 件だったのは、書くときに毎回 `neighboursByDirection`
を回してから書いていたため。**書き方の手順がそのまま品質になっている**
ので、手順のほうを CLAUDE.md の 4 節に移した。

### まだ手を付けていない — 小さすぎる字

Lighthouse の A11y で `font-size`（score 0）が残っている。実測で

    text-[10px]  574 か所
    text-[9px]   286 か所
    text-[11px]  177 か所

`text-[9px]` は Lighthouse の言う「読める大きさ」（12px）を大きく下回る。
ただし**表示の崩れが広範囲に出る**（1,037 か所、100 以上の部品）ので、
機械的に上げると密度の高い盤面が壊れる。**索引に載っている頁
（/houi/area・/houi/pref・/news・/blog・/guide）は既に 10〜11px で、
9px は道具の画面に集中している。**直すなら道具の画面ごとに、
実際の表示を見ながら 1 つずつ。

## 15. 使い方ガイドの棚卸し（2026-08-31）

`/guide` の 7 頁を、実装に当てて 1 節ずつ確かめた。**4 か所が実装と
食い違っていた**（#813〜#815 で修正）。どれも「機能を消したときに
ガイドを直し忘れた」型。

| 節               | 何が書いてあったか                                   | 実際                                                                               |
| ---------------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------- |
| はじめに         | 「5つの道具があります」                              | `CORE_ROUTES` は **9 画面**。時期分析・家賃相場・購入相場・査定の 4 つが抜けていた |
| 物件を方位で探す | 「評価の11の軸」「重み付け10種類」「軸カバー」       | #304〜#308 で**全部廃止済み**                                                      |
| 物件を方位で探す | 「一覧は100件以下のときだけ」                        | #14 で**撤廃済み**                                                                 |
| 物件を方位で探す | バッジは「超大吉・超吉・吉・通常・注意・警告・大凶」 | `maxAstroFactor` に**「超吉」「吉」「注意」「大凶」は無い**                        |
| 物件を方位で探す | 「おすすめ度の★は総合点から決まる」                  | ★は**お気に入り**の印                                                              |

### 直し方で気をつけたこと

**語の一覧をガイドに写さない。**`maxAstroFactor` は 18 通り以上あって
実装側で増減する。写すと 2 か所で持つことになり、また食い違う。
「いちばん強い要因を 1 つだけ出す」という**規則**と例だけを書いた。

### 食い違っていなかったもの（確かめた記録）

- **引越しの日取り**の記号（赦・万・殺・破・合）と点の色（75/60/45 の
  4 段階）… `CosmicCalendar` の実装どおり。**別の部品
  （`AstroGridCalendar`）を見て「違う」と早合点しかけた**。/calendar が
  描くのは `CosmicCalendar` のほう
- **地域を比べる**の「方位スコア 0〜100」「50 点未満は出ない」「80/60 で
  色が変わる」… `municipalities-wealth` と `wealth/page` の実装どおり
- **引越し先を試算する**の 6 段階（大吉〜大凶）と「大凶があると 40 点で
  頭打ち」… `simulator/page` の `rating` と `hasSevereClash` のとおり

### 判断が要る — 移住先マップだけ評価の物差しが違う

CLAUDE.md 4 節には「評価はサイト全体で段階評価（S〜X）の 1 系統だけに
した」と書いてあるが、**移住先の地域を比べる（/relocation/wealth）は
0〜100 の `astrologyScore` を今も使っている。**

    OPTIMAL 100 / SAFE 80 / WARNING 60 / NOISE_VOID・NODE 40 /
    本命殺・的殺・月命殺・月命的殺 20 / 五黄殺・暗剣殺・破 10

判定そのものは同じエンジン（`VectorStatus`）から引いているので**答えは
食い違わない**が、呼び名と刻みが 2 系統ある。並べ替え・絞り込み
（`minScore`）・CSV 書き出し・「50 点未満は出さない」の門がこの数字を
見ているため、S〜X に寄せるなら 4 ファイル以上に及ぶ。

**3 節の手順（旧実装をテストに写す → 広い入力で固定 → 旧挙動で落ちる
ことを確認）を踏まないなら触らない。**踏む前提で、利用者の判断を待つ。

## 16. 「行き止まり」は計算で答えられていなかった（2026-08-31）

市区町村ページ（`/houi/area/[code]`）は候補の入らない方位について、
こう書いていた。

> 市区町村が 1 つも無い方位: 北東・東・南東・南。**海や山で行き止まりに
> なるため**、暦の上でこの方位が吉に出ても引越し先の候補がありません。

**この断定は成り立たない。**`emptyDirections` の母集団は
`src/data/areaDirections.json` に載っている市区町村で、掲載を集計できた
分だけしか無い。

|                                               | 件数           |
| --------------------------------------------- | -------------- |
| `areaDirections.json`                         | **1,119**      |
| `scripts/jis_city_codes.json`（全国の実在数） | **1,917**      |
| 入っていない                                  | **798（42%）** |

北海道は **57 / 194**。釧路町の東は「1 つも無い」と出るが、そこには
**厚岸町・浜中町・根室市が実在する**（3 つとも JIS の一覧にあり、
areaDirections には無い）。美深町・上北山村・喜茂別町も同じ。

県ページには #800 で「巡回がまだ届いていないだけの場合もあります」と
書いたのに、**市区町村ページ側は強い言い切りのまま残っていた。**

### 直したところ

- 見出しを「市区町村が 1 つも無い方位」→「**この一覧に候補が入らない
  方位**」に変え、本文で「その方位に街が無いとは限りません」を明示
  （県ページと同じ文言）
- `areaContent.emptyDirections` の註に、**この関数は行き止まりかを
  答えていない**ことと、母集団の実測を書いた

### 決着 — #835〜#838 で根治した（2026-08-31）

上に「利用者の判断待ち」と書いたデータ源は、**判断が要らなかった。**
`scripts/update_municipalities_coords.ts` が既に
geolonia/japanese-addresses の同じ CSV を引いていて、リポジトリで
使っている出典だった。新しい依存でも課金でもない。

- **#835** … 全国 1,894 市区町村の代表点を `src/data/municipalityCoords.json`
  に入れた（生成は `scripts/build_municipality_coords.ts`）
- **#836** … `emptyDirections` に `hasAnyMunicipality` を足し、空の方位を
  3 通りに分けた。**頁も 3 通りの札で出す**
- **#837** … 真の行き止まりだった 17 頁に、元の強い書き方を返した。
  検査を**禁止から検証に変えた**
- **#838** … 選ぶ基準を「真の行き止まりがある頁」に変えて 3 頁追加
- **#840・#848〜#853** … 同じ基準で 21 頁追加（200 → 221）。空の方位が
  3 通り揃う頁（登別）、行き止まりは 1 方位だけで隣が掲載漏れの頁
  （御坊・柏崎・石狩）、隣に書いた頁がある頁（茂原・坂井・浜田）を
  選んで書き分けた。境目 0.25 度の検査に 2 回掛かった（美祢 0.119 度・
  上田 0.218 度）ので方位に縛らず「ちょうど境目」と書いた。額の検査
  （`/円/`）が「円山川」に誤反応した。検査を緩めず地名の側で避けた

**空の 709 方位の内訳（実測）**

| 種別                             | 件数 |
| -------------------------------- | ---- |
| 遠いだけ                         | 114  |
| 掲載漏れ（街はあるが掲載が無い） | 368  |
| 真の行き止まり                   | 227  |

「行き止まり」と断定していた 595 のうち、**正しかったのは 227 だけ**。
上の「海に面したものは事実」という見立ては**誤りだった**（島がある。
長崎市の西に五島市、福岡市中央区の北西に壱岐・対馬）。逆に、山地を
疑って直した伊勢市の南は**真の行き止まり**で、#837 で再訂正した。
**一律の見立ては両方向に外す。**

### 途中で踏んだもの（同じ轍を避けるため）

1. **緯度経度が空の行。**`Number("")` は 0 で `Number.isFinite(0)` は
   true。素通しすると赤道・グリニッジの点が平均に混ざり、篠栗町の
   代表点が**インド洋（19.2, 74.6）**になった。日本の範囲で弾く
2. **循環参照。**`municipalityCoords` が `AREAS` を読み、`areaContent` が
   `municipalityCoords` を読んで `AREAS is not iterable` で落ちた。
   合流を関数にして呼ぶ側から渡す
3. **合流の向き。**出典側の座標で上書きすると同じ画面の中で方位が
   食い違う（石狩市が札幌市北区の北西＝掲載側／北＝出典側）。
   **掲載側を優先する**
4. **思い込み。**「札幌市北区の北は石狩湾で行き止まり」を検査に書いたら
   落ちた。海岸線が北へ回り込むぶん増毛町（84km）・苫前町・羽幌町が
   真北に入る

### 残っていること

**368 の掲載漏れは巡回の網羅漏れそのもの。**方位の側から測った #47 の
材料になる（北海道は掲載 57 / 実在 194）。巡回設計は 13 節と #47。

## 17. 物件検索が重いのは暦エンジンが client に乗るため（2026-09-01 実測）

利用者から「物件表示までに 20 秒かかっているのか」という問いがあった。
**20 秒は夜間巡回の取得間隔**（`MIN_PAGE_INTERVAL_MS`）で、Web アプリ
とは無関係（`src/` 配下に 0 件、`scripts/nifty_extractor.ts` だけ）。
ただし「物件検索が重い」ほうは事実だったので測った。

### 実測 — 本番ビルドの出力から、頁ごとに実際に読む JS

本番へは到達できない（プロキシが 403。11 節と同じ）ので、LCP は測れて
いない。代わりに #392 で決め手になった指標を測った。`.next/server/app/
<route>.html` の `<script src>` を集めて gzip したもの。

| 頁                      | JS（gzip） | 本数 |
| ----------------------- | ---------- | ---- |
| `/relocation/wealth`    | **361 KB** | 19   |
| `/relocation/arbitrage` | **346 KB** | 21   |
| `/`（ホーム）           | 180 KB     | 13   |
| `/houi`                 | 174 KB     | 11   |

**物件検索はホームの約 2 倍。**ホームが 143 KB で LCP 5.3〜5.7 秒
（11 節）なので、そこに地図と API 往復が乗る物件検索はそれより遅いと
見てよい。**数字そのものは未測定。**

### 中身 — 97 KB は lunar-javascript

arbitrage だけが読む 175 KB のうち最大の 1 本（97 KB、`428ccf76`）を
文字列で確かめたら `Lunar` 42 回・`Solar` 48 回で、暦エンジンだった。

### 「配慮が効いていなかった」わけではない（自分の見立ての訂正）

最初は `arbitrage/page.tsx:52` が `ephemerisEngine` を値として import
しているのが原因、#553 の配慮（`import type` に留めてエンジンを client
に乗せない）が効いていない、と書いた。**これは誤り。**依存を辿ると

    auspiciousDays.ts → utils/lunar.ts → lunar-javascript

で、`judgeDayAllDirections` / `gradeVerdict` を使う時点でエンジンが
乗る。`ephemerisEngine` の 3 つ（`getHonmeiStar` /
`getPersonalVoidZodiac` / `parseDirectionFilterMode`）を外しても
**1 バイトも減らない。**

`auspiciousDays` を読む client 頁は 4 つ（`wealth` `arbitrage`
`simulator` `timing`）。**上位 2 つが重い理由がこれで説明できる。**
つまり取りこぼしではなく、**ブラウザで吉凶を判定するという作りの
帰結**。

### 直すとしたら（未着手・判断待ち）

どれも**画面に出る判定の出方**に関わるので、3 節の手順が要る。

1. **判定をサーバーへ寄せる。**`api/rentals/arbitrage` は既に
   `maxAstroFactor` を計算している。client の再計算が本当に要るのは
   「日付・本命星を変えたときに往復せず出す」ためで、そこを許容できる
   なら一番効く
2. **動的 import に落とす。**初期表示から外し、判定を出す段で読む。
   出方（ちらつき）が変わる
3. **何もしない。**判定が主役の道具なので、エンジンが乗るのは妥当とも
   言える。その場合は**この節を根拠として残す**（次に測った人が同じ
   調査を繰り返さないため）

**測り方を再現する:**

```bash
npx next build --webpack
node -e "…"   # .next/server/app/<route>.html の script src を gzip 集計
```

### 決着 — #882 で 2 案（動的 import）を採った（2026-09-03）

利用者から「初回読み込みが遅い。何をしてもいい」と指示があり、
上の 2 案で直した。**判定の答えは変えていない。**

- 判定（`getHonmeiStar` → `judgeDayAllDirections` → `gradeVerdict` →
  `prefectureDirections`）を `lib/dayKigakuClient.ts` に**そのまま**
  写し、page.tsx は `import()` で遅延して呼ぶ。生年月日と出発地が
  そろって初めて読む
- 型と定数で暦エンジンに繋がっていた 4 本を葉に切り出した
  （`utils/dayTier`・`utils/directionFilterMode`。`DIRECTION_LABELS` は
  `directionGeo` にある）。**ラベル 1 つを `kigakuContent` から取った
  だけで 97 KB が戻る**ので、`__tests__/arbitrageBundleLeaf.test.ts`
  が page.tsx から値 import を辿って lunar / astronomy に届かないことを
  固定する（字面の grep は #552 の轍を踏むので、import を辿る）
- 旧 `useMemo` の中身を `__tests__/dayKigakuClient.test.ts` に写し、
  864 通り（生年月日 3 × 出発地 3 × 日付 4 × 見方 4 × 天中殺 3 ×
  盤 2）で一致を固定した

**実測（同じ環境の前後。`.next/server/app/relocation/arbitrage.html` の
script を gzip 集計）**

|     | JS（gzip） | 本数 |
| --- | ---------- | ---- |
| 前  | 347 KB     | 21   |
| 後  | **217 KB** | 19   |

Chromium（CPU 4 倍・fast 3G 相当）で `next start` を開いた実測は
`load` 4.9 秒 → **2.9 秒**、Leaflet の器が出るまで 6.1 秒 → **4.2 秒**。
ホーム（180 KB）とほぼ同じ重さになった。

### hydration 後にも取りに行っていた（#883）

上の数字は最初の HTML の script だけ。開いてから 10 秒待って取りに
行った塊を全部並べると、**設定の無い利用者でも暦エンジンの塊を
取りに行っていた。**`DirectionTierOverview` を無条件に置いていた
ためで、行が空なら `null` を返すが、置いた時点で `honmeiYear`（→
`AstroEngine` → lunar）ごと読む。行が空のときは置かないようにした。

実測（`next start` を Chromium で開き、12 秒待って要求した JS を全部
数える）。**「置くだけで読む」ぶんは消えた。**

| 設定              | 経路の prefetch を止めた状態              | 暦エンジンの塊           |
| ----------------- | ----------------------------------------- | ------------------------ |
| 無し              | 24 本（初回 16 + 地図の遅延 8）           | **取りに行かない**       |
| 生年月日 + 出発地 | 初回 16 の直後に `dayKigakuClient` が読む | 取りに行く（判定に要る） |

ただし**何もしなくても 10 秒で 48 本・762 KB を取りに行っている。**
サイドバーの `Link` が画面内の経路（暦・時期・資産・シミュレータ…）
を prefetch するためで、そちらが lunar（97 KB）と recharts（95 KB）
を含む。優先度は全部 `Low`、しかもモバイルではサイドバーが閉じて
いて画面内に無いので発火しない。**この頁の遅さの原因ではない**ので
触っていない。止めるなら `prefetch={false}` をサイドバーに置くだけ
だが、道具の間の移動が 1 回ぶん遅くなる。

### 遅い回線で開かない、の実測と対処（#884）

利用者から「他の競合よりとても遅い。ネットワークが遅いところだったら
開きもしない」と指示があり、遅い回線（400 kbps・RTT 400 ms・CPU 4 倍。
Chrome の Slow 3G 相当）で全要求を記録した。**描画を止めていたのは
HTML でも JS でもなく、フォントだった。**

| 何が                              | 大きさ（gzip） | どう効いていたか                                        |
| --------------------------------- | -------------- | ------------------------------------------------------- |
| 明朝体の @font-face（2 ウェイト） | 62 KB          | 全頁の stylesheet に入り、描画を止める（1.3 秒）        |
| Geist 2 書体の preload            | 52 KB          | 最優先で落ちて CSS と JS の帯域を先に食う（1 秒）       |
| 明朝体の実体（見出しの文字ぶん）  | 104 KB         | 描画直後に最優先で落ちて hydration の JS と競合（2 秒） |
| サイドバーの Link の prefetch     | 500 KB 前後    | 15 頁ぶんの JS を裏で取りに行く                         |
| supabase（ログイン表示のため）    | 60 KB          | ログインしていない端末でも読む                          |
| framer-motion（通知 1 つのため）  | 39 KB          | 地図の塊に乗る                                          |
| 県の輪郭 geojson                  | 141 KB         | 俯瞰でしか描かないのに開いた時点で読む                  |

明朝体は `lib/serifFont` に移して hydration 後に `SerifFontLoader` が
読む（届くまでは端末の明朝体）。Geist は preload を切った。Link は
`prefetch={false}`。supabase は `sb-<ref>-auth-token` の cookie が
無ければ読まない。framer-motion は CSS の keyframe に、geojson は
俯瞰に入ってから。

|                                | 前      | 後          |
| ------------------------------ | ------- | ----------- |
| 物件検索: 最初の描画（FCP）    | 5.85 秒 | **2.08 秒** |
| 物件検索: 操作できる（DCL）    | 6.28 秒 | **2.03 秒** |
| 物件検索: load                 | 11.6 秒 | **7.2 秒**  |
| 物件検索: 地図タイルの要求開始 | 16.6 秒 | **8.0 秒**  |
| ホーム: FCP                    | 4.61 秒 | **2.22 秒** |
| ホーム: load                   | 13.6 秒 | **6.3 秒**  |

最初の HTML が読む CSS は 89 KB → 27 KB。**全頁に効く。**

**測り方を再現する:** `next start` を立てて Playwright の Chromium で
開き、CDP の `Network.emulateNetworkConditions`（400 kbps / 400 ms）と
`Emulation.setCPUThrottlingRate`（4）を掛け、`Network.requestWillBeSent`
と `loadingFinished` で全要求の大きさと時刻を取る。FCP は
`performance.getEntriesByName("first-contentful-paint")`。

### 残っていること

- **API の走査時間は別の話。**`/api/rentals/arbitrage` の応答に掛かる
  時間は本番でしか測れず、Cloud Run のログをこの環境から読む手段が
  無い（`check-logs.yml` は VM の PM2 用）。19 節の `db-explain.yml`
  は本番 DB に負荷を掛けるので利用者の判断が要る。画面の「走査時間
  DB / 判定」の表示から読める
- **AdSense のスクリプト**（`<head>` の async）。この環境では鍵が無く
  測れていない。本番では adsbygoogle.js とその後続が最初から落ちて
  くる。**収益に関わる**ので触っていない。読み始めを load 後まで
  遅らせれば遅い回線の操作可能までは縮むはずだが、表示回数への影響は
  測らないと分からない
- **Geist そのもの**（52 KB）。preload を切っても、文字が出た直後に
  最優先で落ちてきて JS と 3 秒ほど競合する。端末のフォントにすれば
  消えるが、見た目の判断
- **JS の底**（react-dom 61 KB・router 50 KB・頁 27 KB ≒ 190 KB）。
  これ以上は頁の分割か、判定以外を SSR に寄せるかで、別の話

## 18. 夜間のデータ更新が検査を壊しても、誰も気づかなかった（2026-09-01）

巡回のあと `areaDirections.json` が更新されると、**それを読む側が
壊れることがある。**実際に起きた。

    長岡市         → 小千谷市: 文章=南西  頁の一覧=南
    名古屋市中川区 → 弥富市  : 文章=西    頁の一覧=南西
    周南市         → 岩国市  : 文章=北東  頁の一覧=東

代表点は**掲載物件の緯度経度の平均**なので、巡回で掲載の分布が変わると
点が動き、方位の区分をまたぐ。手で書いた市区町村ページの文章が古くなる。

### 気づけなかった経路

    巡回 → git push origin HEAD:master → deploy.yml → 本番
                                      ↘ ci.yml（赤くなるが、何も止めない）

`ci.yml` は master への push でも走るので**赤にはなっていた**。しかし
`deploy.yml` は独立していて、CI の結果を見ない。**赤い master のまま
デプロイされる。**

気づいたのは偶然（別件で CI の一覧を見ていた）。その間 **09:13〜10:15 の
約 1 時間**、頁の一覧と文章が食い違ったまま公開されていた。

### 直し方 — 押すのは止めず、赤くする

`scrape-rentals.yml` の geocode ジョブに、**push のあとで**テストを
回す段を足した（#846）。落ちたらジョブを失敗させる。

**push の前に置いて止める案は採らなかった。**壊れるのはデータではなく
**それを読む側の文章**で、止めると相場が一晩ぶん古いままになる。
相場が古いほうが利用者には害が大きい。

検査の一覧はワークフローに書かない。データを読むテストは 16 ファイル
あり、**書き写すとその一覧のほうが古くなる**（このリポジトリで何度も
起きた型）。全部回す。geocode は既に 1 時間以上かかっており、2〜3 分の
追加は問題にならない。

### 訂正 — 3 件とも「境目の揺れ」だった（#847）

#844 で「弥富・岩国は区分の真ん中で、動く余地が無い。単に間違い」と
書いたが、**誤りだった。**境目の値を実装から読まず、45 度等分の値
（15/45/105/135…）を混ぜて測っていた。正しくは伝統区分の
**15/75/105/165/195/255/285/345**。測り直すと

    長岡市 → 小千谷市        194.951°  境界（195°）まで 0.049°
    名古屋市中川区 → 弥富市  254.982°  境界（255°）まで 0.018°
    周南市 → 岩国市           75.004°  境界（75°）まで  0.004°

**3 件とも境目の真上。**落ちるペアが境目に集中するのは偶然ではなく、
**境目付近のペアしか反転しないから**（選択の効果）。

### 「赤いまま」を常態にしない

当初は「鮮度を取る＝CI は赤のまま」と整理したが、**これも誤り。**
赤い master を常態にすると、上で足した警報が次の本物の故障のときに
無視される。**鮮度も取るし、CI も緑に戻す。**

そのために検査の側を直した（#847）。境目から **3 度**以内なら両隣
どちらの表記も通す。根拠は代表点の移動の実測（5 版・33,656 ペア）。

    点の移動      中央 0〜9m   95% 104m   最大 1.81km
    方位角の振れ  中央 0.025°  95% 0.220°  99.9% 1.487°  最大 2.975°

**3 度を超えた振れは 1 件も無い。**#844 以前の文章に当て直すと通り、
区分の真ん中で方位を間違えた文章（大田市 → 三次市を「北」）は落ちる
ことを両方向で確かめた。

### 残っていること

- **deploy.yml が CI を見ない**のは直していない。ただし上の対応で
  「毎晩ぶれて赤くなる」経路は塞いだので、**これからの赤は本物**に
  なる。CI を見るようにするかは、それを見てから決められる

### 本筋のほう — 境目の地名を方位に縛らない（#846）

margin は「CI を無駄に赤くしない」ための措置で、**読み手の実害はそれ
では消えない。**頁の一覧はその日の掲載で決まるので、境目のすぐ近くに
いる相手は文章と半々で食い違う。

そこで**書く側の決め事**にした。全 1,189 件の地名を照合して分布を出す。

| 境目まで   | 件数 | 割合  |
| ---------- | ---- | ----- |
| 0.25° 以内 | 10   | 0.8%  |
| 0.5° 以内  | 28   | 2.4%  |
| 1° 以内    | 52   | 4.4%  |
| 2° 以内    | 124  | 10.4% |
| 3° 以内    | 161  | 13.5% |

**線は 0.25 度。**方位角の振れは 95% が 0.220 度なので、それより境目に
近ければ**ふつうの夜の揺れで反転する。**3 度で切ると 161 件になり、
そこまで削ると文章から地名がごっそり消えて読み物として痩せる。

該当した 10 件は全部直した。

    南相馬市の本宮 0.004°   沼津市の天竜 0.074°   由利本荘市の弘前 0.077°
    市川市の荒川 0.088°     熊本市東区の日田 0.095°  甲斐市の飯田 0.098°
    久留米市の上峰 0.168°   博多区の早良 0.169°    大田区の目黒 0.194°
    八戸市の花巻 0.243°

7 件は並びから名前を外した。**近くて目立つ 3 つ（日田・早良・目黒）は
消さずに、どの方位の境目にあるかを書いた**（消すと読み手が失うものが
大きい）。境目の両隣は計算で出していて、手で書いていない。

検査は**丸めた bearing ではなく実際の方位角**で測る。丸めると 0.5 度の
誤差が入り、境目の真上と区別が付かない（最初の測定でそれをやって
「25 件が境目の真上」という誤った数字を出した）。

### 残っていること

- **deploy.yml が CI を見ない**のは直していない。ただし上の 2 つで
  「毎晩ぶれて赤くなる」経路は塞いだので、**これからの赤は本物**に
  なる。しばらく運用して赤が出ないことを確かめてから決めればよい

## 19. 俯瞰にすると物件検索が遅い（診断のみ。2026-09-01）

利用者の報告。「俯瞰にしたら出なくなるというより遅い」。本番は測れないので
実装から経路を追った。

### 走る条件

`page.tsx` は **県も半径も無し × zoom < 10** のときだけ走査を止める
（全国 45 万行の名寄せ。実測 18.4 秒、#217）。それ以外の俯瞰——
z=10〜12 の広い bbox、あるいは県か半径を指定した状態——は毎回走査する。

しかも前回の一致件数が上限 500 を超えていると（写真は 11,457 件）、
`lastTotal <= dataLimit` が偽になり**範囲を動かすたびに再走査**になる。
これは正しい（上限に当たった窓は広い範囲の上位 500 なので、狭めた範囲の
上位 500 とは限らない）。

### 遅い理由は SQL の形

    innerSql:  SELECT DISTINCT ON (name_key, floor, layout, size_sqm, rent) …
               ORDER BY <名寄せ鍵>, <代表行の選び方>
    selectSql: … ORDER BY percent_rank() OVER (ORDER BY sqm_rent) * 0.4 + …
               LIMIT 500

`DISTINCT ON` と `percent_rank() OVER` は、**bbox 内の全行を実体化して
ソートしてからでないと LIMIT を掛けられない。**俯瞰で bbox が広がるほど
行数に比例して遅くなる。並行して同じ行を `uniqueCountSql` が GROUP BY で
もう 1 回数える。work_mem を広げても速くならなかった（#296。律速は CPU）。

描画側は原因ではない。ピンは画面内だけ（`pinProperties`）、まとまりも
`visibleCount <= 100` のときだけ作る。

### 直すなら（判断待ち）

1. **bbox の面積で止める。**いまは県／半径の有無で判定しているが、
   遅さは面積で決まる。一定以上の面積では走査せず、県別・表示範囲の
   件数（`prefecture-counts` / `viewport-count`。どちらも索引の範囲引きで
   軽い）だけを出す。**「遅い」が「出ない」に変わる**ので利用者の判断が要る
2. **並び順の窓関数を LIMIT の後に回す**（先に sqm_rent だけで上位 N を
   取り、その中で 4 軸の percent_rank を出す）。答えが少し変わる
   （3 節の手順が要る）
3. 本番の EXPLAIN を取る。`db-explain.yml` が `prefecture=all` の最悪
   ケースを流せる。**本番 DB に数十秒の負荷を掛ける操作**なので投入は
   聞いてから

### 続報: 本番の実測（2026-09-11。`scan-timings.yml`、#1177）

「本番は測れない」を測れるようにした。走査 API は 1 回ごとに
`arbitrage: db Xms / compute Yms (pref= radius= rows=a/b …)` を
console.log しているので、それを 48 時間ぶん読んで分布にした。
座標も生年月日もログに無い（run-error-logs と同じで httpRequest は
読まない）。

直近 48 時間・88 回:

| 区間 | 中央値 | 9 割点 | 最大 |
| --- | --- | --- | --- |
| DB（候補の切り出し） | 2,339 ms | **25,494 ms** | 40,641 ms |
| 計算（判定・スコア） | 345 ms | 565 ms | 886 ms |

- `pref=all` が 85 回（9 割点 25.5 秒）、県指定は 3 回（中央値 4.8 秒）
- 上限 500 に当たったのは 75 / 88 回
- 当たった総行数は中央値 25,582、9 割点 **334,118**
- 遅かった順: 40.6 秒（all・radius 0・305,200 行）、33.7 秒（all・
  **radius 50**・334,118 行）、…、17.1 秒（127,584 行）、14.1 秒（49,742 行）

**分かったこと。**

1. **遅いのは DB で、計算ではない。**計算は最大でも 0.9 秒。Rust に
   書き直しても速くならない（Comfy の記事の「体感はほぼ変わらない」と
   同じ話）
2. **#1159 の前提「半径が選ばれていれば母数が絞られる」は誤りだった。**
   首都圏で radius=50 にすると 33 万行に当たる。面積の規則
   （`shouldPauseScan`）は県／半径が指定されていると免除するので、
   いちばん重い走査がすり抜けていた
3. **db-explain の 880 ms と本番の 17 秒（同じ 12.7 万行）は 20 倍違う。**
   原因は特定していない（本番の同時実行、work_mem、コールドキャッシュ、
   Cloud SQL の tier のどれか）。db-explain の 1 点測定を速さの根拠に
   しないこと

**打った手。**上の「直すなら」の 1 を行数で言い直した。

- #1178: API が `count` を先に取り、`SCAN_ROW_LIMIT`（10 万行）を超えたら
  切り出しを走らせずに `metadata.paused` を返す。`force=true` で通す。
  count を先に取るので並行していた 2 本が直列になり、上限以下の走査は
  count のぶん遅くなる（中央値 2.3 秒の側）。上限超の走査は 17〜41 秒が
  0 秒になる
- #1179: 画面が `force` を送り、止めた応答を面積の止めと同じ札で出す
  （行数と実測の秒数を書く）。前の一覧は消さない

**次に見ること。**1 日置いて `scan-timings.yml` をもう一度回す。見るのは
DB の 9 割点（25.5 秒からどれだけ下がるか）と、`arbitrage: paused` の
回数（止めた走査がどれだけあり、`force` で押し通した人がどれだけいるか）。
「直すなら」の 2（窓関数を LIMIT の後に回す）は答えが変わるので、
やるなら 3 節の手順で。

### 続報 2: 20 倍の正体は「同じ走査が 2 本同時に走っていた」（2026-09-11）

上の「原因は特定していない」を同日中に詰めた。順に外れた仮説も残す。

**仮説 1: 夜間の巡回と重なっている → 外れ。**#1181 で時刻（JST の時）を
足して同じ 88 回を分けたら、巡回中（04〜10 時）のほうが速かった。

    巡回中  n=30  median=771ms    p90=6,502ms   max=8,123ms
    巡回外  n=58  median=3,159ms  p90=29,476ms  max=40,641ms

**仮説 2: Prisma の generic plan → 外れ。**`@prisma/adapter-pg` で文に
名前を付けていない（`statementNameGenerator` 未設定）ので、毎回 custom
plan。

**分かったこと 1: 遅い回は全部が 1〜2 秒差の対だった。**#1182 で秒まで
の時刻を足した。

    33,693 ms  10:37:07Z    32,229 ms  10:37:06Z   同じ条件・334,118 行
    29,476 ms  10:39:16Z    27,786 ms  10:39:15Z
    40,641 ms  03:33:32Z    39,375 ms  03:33:30Z
    17,062 / 14,141 / 11,536 ms  03:33:08〜10Z（3 本）

ログは走査の**終了**時刻なので、同じ走査が 2 本、DB で同時に走っていた。
1 本ずつなら db-explain の 5 秒前後（52〜56 万行で 4.5〜4.9 秒）で済む
ものが、重なって 25〜40 秒になっていた。**「本番は 20 倍遅い」のでは
なく、「本番は 2 本ずつ走らせていた」。**

2 本になる経路は画面側。頁を開くと条件の確定（100ms）と地図の表示
範囲の報告（500ms）で 2 本、出発地を変えても地図が動いて 2 本、パンを
続ければ 500ms ごとに 1 本ずつ。古い応答は `fetchSeqRef` で捨てるが、
DB は捨てられたぶんも最後まで走る。→ #1184 で `lib/latestWinsQueue`
（走っている間の要求は最新の 1 つだけ取っておく）に 1 本化。

**分かったこと 2: 半径があると表示範囲を捨てていた。**画面は表示範囲を
「半径への追加の絞り込み」として送っている（#1156）が、API は Prisma
where と `buildWhereSql` の両方が `if (半径) … else if (表示範囲) …`。
東京・半径 50km では地図をどこへ動かしても同じ 334,118 行になり、500 件
の窓も半径全体の安い順なので写っている範囲の物件が出なかった。
→ #1183 で `geoBoundingBox`（交差）を 1 つ置いて両方から読む。
**#1159 の「半径が選ばれていれば母数がそちらで絞られる」は、この
不具合を前提にした誤りだった。**

**分かったこと 3: 広い範囲は名寄せの索引で表全体をなめる。**#1181 で
計画を全部出した（run 34612331430）。半径 150km（52〜56 万行）は
`Index Scan using rental_properties_name_key_floor_layout_size_sqm_rent_idx`
で 170 万行を索引の並びで読み、`Rows Removed by Filter: 1,198,941`。
名寄せの並べ替えを省く代わりに全行を読む（Buffers 167 万ページ、
全部 shared hit）。半径 30〜50km（13〜19 万行）は `lat_lon_idx` の
Bitmap → Sort → WindowAgg → Incremental Sort で 1.4〜1.9 秒。planner の
行数の見積もりは実測の 10〜28 分の 1（lat と lon の範囲を独立に掛ける
ため）。#1178 の門（10 万行）が効いている間は当たらない。直すなら
「直すなら」の 2 と同じで 3 節の手順が要る。

**次に見ること。**#1183・#1184 が出てから 1 日置いて再計測。
`arbitrage: db` の対（1〜2 秒差）が消えているか、9 割点がどこまで
下がるか。db-explain の D は今回 1,913 ms（前回 880 ms）で、直前まで
scrape-rentals-new（run 171、14:48:55 終了）が走っていた。書き込みの
直後は 2 倍遅い、という程度の影響はある。

## 20. MCP の口を足した（2026-09-01）

`/api/mcp`。Streamable HTTP、stateless、POST のみ、SSE 無し。中身は
`lib/mcpServer`、検査は `__tests__/mcpServer.test.ts`（in-memory transport
で道具を直接呼び、**画面と同じ関数を同じ入力で呼んだ結果と突き合わせる**）。

道具は 6 つ。全部 in-process の計算と静的 JSON で、DB に触らない。

    get_honmei_star       本命星と天中殺の干支
    judge_directions      指定日の八方位の段階（S〜X）と状態
    find_auspicious_days  期間内の吉日を方位ごとに集計
    search_municipality   名前から市区町村コードを引く
    area_directions       市区町村ページと同じ方位別一覧（空の方位は 3 分類）
    prefecture_summary    県ページと同じまとめ

**公開範囲は広げていない。**返すのは匿名で画面から得られるものだけ。
物件の走査（数秒・DB）は載せず、案内だけ返す。

依存は `@modelcontextprotocol/sdk` 1.30.0（zod v4 と互換）。

---

## 21. 旧ポータルのサブドメインを 4 本畳んだ（2026-09-02）

利用者から「`katmer.cloud-palette.com` を SEO の観点でどうするか」と
相談があり、調べたら**旧ポータルの残骸が DNS に 4 本生きていた。**

    brain.cloud-palette.com     CNAME ghs.googlehosted.com
    fortune.cloud-palette.com   CNAME ghs.googlehosted.com
    katmer.cloud-palette.com    CNAME ghs.googlehosted.com
    tech.cloud-palette.com      CNAME ghs.googlehosted.com

`src/app/page.tsx` のコメントに残っている「以前は『知性と感性をかさねる
ポータル』で、3 つのサブドメイン（占い・株トレンド・ナレッジベース）への
ランチャーになっていた」が、そのままの形。fortune が占い、tech が株
トレンド、brain と katmer がナレッジベース。

### 何が問題だったか

**索引されていたのは katmer のルート 1 枚だけ**（`site:` で実測）。
ただしその 1 枚の説明文が

> 自分らしく、心地よく。知性と感性をかさねる場所. AIタロットによる
> 毎日の気づき、アイディアを育てるノート、そして最新のトレンドインサイト。

で、**サイトが名乗るのをやめた立ち位置**を同じドメイン配下で名乗り続けて
いた。cloud-palette.com は AdSense に「有用性の低いコンテンツ」でサイト
全体の配信を止められた履歴があるので、これは持っていたくない。

Search Console は**ドメインプロパティ**（`cloud-palette.com`）なので、
サブドメインの URL も同じレポートに混ざる。市区町村ページを索引に戻す
判断（#750〜）の材料が濁っていた。

### 直し方の判断

- **パスに寄せない。**「サブドメインよりサブディレクトリのほうが評価が
  集まる」は関連する内容ならの話で、無関係な道具を寄せると主題が濁る。
  #379 と llms.txt の書き直しでずっと絞ってきた方向と逆
- **robots.txt の Disallow では足りない。**サイドバーから全ページで
  リンクしていたので、クロールを止めると本文を読めないまま URL だけが
  索引される。「クロールは許す + noindex」が正しい組み合わせ
- 最終的に**畳む**と決まったので、DNS を消した。マッピングがどの
  リージョンにあっても名前解決が止まる

### 実際にやったこと

    #873  katmer-defuddle/knowledge-base に noindex（metadata + ヘッダ）
    #874  GlobalSidebar から Katmer への導線を削除
    #875  QA 手順書 3 ファイルから記述を削除（ついでに 5 項目 → 3 群 10 本）
    手作業 Cloudflare の DNS から 4 本削除（18 → 15 レコード）

**#873 は索引されていた頁を覆っていない。**リポジトリの
`knowledge-base`（英語で "Your second brain…"）と、katmer のルートに
出ていた旧ランディング（日本語）は別物だった。noindex を入れた先が
違うと気付かないまま報告し、あとで訂正している。**ホスト名から
リポジトリの中身を推測しない。**

### 残り（手作業。急がない）

- Search Console → ドメインプロパティ → 削除 → プレフィックス
  `https://katmer.cloud-palette.com/`（**索引されていたのは katmer だけ**）
- Cloud Run の後片付け。ドメインマッピングは**リージョンごと**で、
  コンソールの一覧は 1 リージョンぶんしか出ない。4 本のマッピングは
  us-central1 にあるはず（`knowledge-base-app` が居るリージョン）
- us-central1 の `portfolio-app` も残骸（本番は asia-northeast1。
  最終デプロイ 2026-08-08・0 req/s で ¥84 出ている）。課金なので判断待ち

---

## 22. Search Console の実測で次を決めた（2026-09-02）

利用者から「参考にして分析してサイトを改善して」とレポート（CSV）を
受け取った。期間は 6/1〜9/2 だが、表示が付くのは **8/14〜31 の 18 日間**
だけ。合計 **348 表示・9 クリック**、モバイル 255 / PC 91。

### 何が来ていたか（検索語の群）

    「◯◯市 家賃相場」の類     深谷 11・千葉 10・愛知 12・埼玉 5 …  35〜50 位
    天中殺 / 大殺界             「大殺界と天中殺の違い」8 表示 10 位、ほか 15
    日取り                      「日取り」「日取りとは」            30〜45 位
    本命殺 / 本命的殺           各 1                                11〜49 位

頁の側で見ると、表示の多い順に

    /blog/tenchusatsu-names-and-schools   31 表示  18.3 位  **0 クリック**
    /houi                                 90 表示  11.6 位  1 クリック（CTR 1.1%）
    /houi/pref/23（愛知）                 20 表示  44 位
    /houi/pref/12（千葉）                 18 表示  45 位
    /calendar/2027-07                     13 表示   7.3 位  0 クリック
    /houi/area/11218（深谷）              11 表示  37 位   ← **noindex の頁**

### 読み取ったこと

1. **需要は「家賃相場」に来ている。**方位ではない。#379 で全部
   noindex にした市区町村頁が、noindex のまま「深谷市 家賃相場」で
   37 位に出ている。書いて index に戻せば上がる余地がある頁がここ
2. **順位 10 位で 0 クリック**の記事がある。検索語は「違い」なのに
   title は「同じものか」。答えが title に無い
3. `/houi` は 1 ページ目の縁。検索語の大半は年を含むはずだが title に
   年が無い

### やったこと

- **市区町村 10 頁に文章を書いて index に戻した。**深谷・諫早・筑前町・
  佐倉・草加・扶桑町・八女・長久手・直方・伊東。**選ぶ順序を
  Search Console の表示の多い順に変えた**（5 回目。これまでは掲載数 →
  県の偏り → 空の方位 → 真の行き止まり。決め事の「需要のある頁から」
  に初めて実測の根拠が付いた）
- 記事の title を「天中殺と大殺界の**違い**は何か」に。description も
  先頭で答える形に
- `/houi` の title に年を入れた（`generateMetadata` で
  `currentYearInJapan()`。h1 は変えない）

### 触らなかったもの

- 県ページ（愛知・千葉・埼玉）が 44 位前後なのは、title や description
  の問題ではない。文章は既にある。上げるなら中身（間取り別の相場など）
  で、別途
- `/calendar/2027-07` の 0 クリックは母数 13。様子見
- 「日取り」は一般語で、単独で上位は難しい。追わない

### 次に見るとき

表示は 8/26〜31 に伸びている（34・28・28・22・**70**・41）。8/30 の
山は #750〜 の索引戻しと重なる。**次のレポートでは、今回書いた 10 頁が
「◯◯市 家賃相場」で何位に出るか**を見る。上がらなければ、文章では
なく頁の作り（相場の表を上に、間取り別の中央値を出す）の問題。

## 22. 地図の名所と用途地域の俯瞰（2026-09-05）

利用者の要望 3 つ（「用途地域も全国の俯瞰で見たい」「地図を速度を
落とさずに充実させたい。パワースポットをポップアップで」「ホームの
BIO-LOCATION SIMULATOR はもう載せなくていいのでは。重い」）を
同じ日に進めた。

### ホームの節を頁に移した（#984・#985）

`SolarTimeClock`（7 タブ）は他の頁に無い中身なので消さず、
`/relocation/dashboard` に移してホームの札 1 枚から辿る形にした。
**同じ環境・同じ手順で外す前後を実測**:

    ホームの遅延チャンク   457KB（4 個） → 0KB
    ホームの静的チャンク    74KB（7 個） → 69KB（7 個）
    rootMainFiles          384KB → 384KB

木の全体（地図・図・パネル・数式を含む）は 1,691KB（25 個）で、
これが dashboard を開いた人にだけ配られる。

当初「7 タブのうち 3 つは他の頁にある」と報告したが**誤り**
（履歴・タイミング・プロフィールは名前が同じだけで別物）。訂正済み。

### 名所の層（#981〜#991）

- 一覧は Wikidata（CC0）。**QID もプロパティも probe で実測したもの
  だけ**（一宮を P31 / P1435 で引いて 0 件だった。推測で並べない）

      一宮     P13723 → Q1656379    106 件（座標あり）
      特別名勝 P1435  → Q94987823    40 件
      名勝     P1435  → Q11414752   275 件

  計 421 件。座標の無い 22 件は名前で出す（黙って落とさない）。
  「大日本国一宮記」は神社ではなく書物で、Wikidata 側の指定が誤り
- 判定は新しく作らない。方位は物件・県・`SpotVerdict` と同じ経路、
  段階はページが組んだ盤のセルを借りる（`lib/powerSpots`）
- 既定は非表示で、一覧（54KB）は押されてから読む。色の意味は
  2 つだけ（一宮＝琥珀、名勝＝緑）
- 効果や利益は書かない。テストが spots の文字列を見る
- 利用者登録地点は**端末の localStorage だけ**（`lib/userSpots`。
  サーバーに送らない＝個人情報を増やさない）

### 用途地域の俯瞰 — タイル API では組めない（#987・#988・#990）

probe で z5〜10 を投げたら全部 **HTTP 400「不正なズーム値」**。上流の
下限は z11 で確定（404 ではなく 400 なので、ズームの非対応は本文で
見分けられる）。俯瞰の 1 画面 = z11 の数千枚なので、タイル API からは
組めない。

代わりに**国土数値情報 A29（用途地域）の一括ファイル**が使える形:

    年版: A29-11（2011）と A29-19（2019）。各 47 県
    大きさ（A29-19）: 北海道 9.1MB・東京 45.6MB・大阪 11.5MB
    中身（鳥取で実測）: 市町村ごとに shp / XML / GeoJSON（経度緯度）
    属性: A29_004 = 用途地域の番号（上流 XKT002 の youto_id と同じ体系）
          A29_005 = 名前（漢数字。上流は全角数字）
          A29_006 / 007 = 建蔽率 / 容積率
          A29_008 = **条件付き公開**

`lib/zoningRaster` の塗り（youto_id → 色）をそのまま当てて、一度だけ
z5〜10 の絵に焼くことは技術的にできる。**判断待ち:** `A29_008 =
条件付き公開` の条件と利用規約（nlftp.mlit.go.jp/ksj/other/agreement.html）
を人が読む。出典の明示は地図の帰属表示に足す。上流（XKT002）の年版と
A29-19 が同じかも未確認（俯瞰だけ古い版になる可能性）。

### 駅・路線 — 国土数値情報 N02 が使える形（#997）

利用者の順は「駅・路線は最後に、測ってから」。probe で測った:

    年版: 2005〜2025 の 20 年版。全国 1 本ずつ（2025 年版 14.9MB）
    中身（N02-25）: UTF-8 の GeoJSON が 2 本。座標系 EPSG:6668
      RailroadSection  14.2MB  21,933 本の LineString（路線）
      Station           3.3MB  10,234 本の LineString（駅。点ではなく線分）
    属性: N02_001 鉄道区分 / 002 事業者種別 / 003 路線名 / 004 運営会社
          / 005 駅名 / 005c 駅コード / 005g グループコード

駅の層を作るなら線分の中点を点にしてグループコードで畳む（9 千前後）。
1 駅 80 バイト前後で全国数百 KB——**名所（68KB）の 10 倍**。路線
（14MB）は client に載せない。**判断待ち:** 利用規約・公開条件を
人が読む（A29 と同じ）。読んだら、駅の JSON を焼いて押されてから
読む層にする。

### 判断が出た後（2026-09-05 午後）

利用者の判断: 「A29 はお金が掛からないならなんでもいい。駅も公開できる
ならしてほしい」。国土数値情報は無償で、メタデータの制約は
「利用に当たっての制限は、国土情報利用約款を参照してください」だけ
（両方の取り込みログに残してある）。出典の明示を帰属表示に載せた。

**駅（#999・#1001・#1003）**

    取り込み: N02-25 の Station.geojson 10,234 本 → グループコードで畳んで
              9,046 駅・596 路線（run 33955206982、bot の 1bbd895）
    遅延チャンク: 692KB（押すまで配られない。名所の 10 倍なので既定は非表示）
    層: StationLayer（共有部品）。z11 未満は升目。3 つの地図に「🚉 駅」

**用途地域の俯瞰（#1000・#1002）**

    焼き込み: A29-19 を 47 県（GeoJSON 1,150 本・104,702 区画・読めなかった
              もの 34）→ z5〜z9 で 257 枚・0.5MB、18 秒（run 33956768133、
              bot の e385492）
    z10 は焼かない（API の画像タイルが z11 を縮めて受け持つ。焼くと枚数が
    4 倍。鳥取の試運転で z10 が 18 枚中 7 枚）
    層: ZoningLayer に /zoning-overview/{z}/{x}/{y}.png の TileLayer。
        無いタイルは透明。1 区分の絞り込みは俯瞰に効かない（案内で拡大を促す）

**その過程で見つけた 2 件**

- 夜間の集計更新（d205a3f）で市区町村の文章 8 頁が実測と外れ、master の
  CI が赤になった（18 節と同じ形）。#999 で直した
- 駅名に運営者の住所の町丁名と同じものが偶然あり、個人情報の見張りが
  鳴った。公開データの駅名であって住所の漏れではないので、
  stations.json をファイル名で見張りから外した（#1001）。**見張りに
  個別の名前を書かない**——書くとそれ自体が漏れになる
- tee はパイプの左の失敗を隠す。焼き込みの枚数の下限が ::error を出し
  ながら job が緑だった。ワークフローの run は `set -o pipefail` を付ける

### 教訓

- **probe は 1 回で終わらない。**一覧ページの zip は 0 本（絶対パス
  だけ探していた）→ 手がかりを出す → 相対パスだった → 拾い直す、で
  3 回。推測で URL を組まず、毎回「中身を出して次を決める」
- ワークフローの `workflow_dispatch` は、**ファイルが既定ブランチに
  無いと 404**。新しいワークフローは 1 度マージしてから枝で回す

---

## 23. 本番のエラーログを初めて読んだ（2026-09-08）

`roles/logging.viewer` を貰い、`run-error-logs.yml` が通るようになった
（run 34224779578）。**直近 24 時間で 23 件。**

    18  Error: Internal: NoFallbackError … app/houi/[year]/[star]/[month]/page.js
     3  Memory limit of 512 MiB exceeded with 512 MiB used
     1  Error: Internal: NoFallbackError … app/houi/area/[code]/page.js
     1  Error: Internal: NoFallbackError … app/houi/[year]/[star]/page.js

### NoFallbackError（20 件）— 片付いた。**実害は無かった**

最初は「消えた URL が 500 を返している」と疑ったが、**それは誤り
だった。**本番と同じ standalone のサーバーをローカルに立てて実測すると、
`/houi/2025/1/3`・`/houi/2024/1/3`・`/houi/2025/1`・`/houi/area/99999`
はいずれも**前から正しく 404** を返していた（生きている頁は 200）。

原因は `dynamicParams = false`。経路の側で弾くと、Next は弾くたびに
`NoFallbackError` を **severity=ERROR** で吐く。番号は正しいので
利用者にも検索エンジンにも実害は無く、**ログだけの雑音**だった。

同じビルドの中で `/blog/[slug]`（元から `dynamicParams = true`）が
対照になる。同じ 404 を返して、この記録を 1 件も吐かない。
`#1135`・`#1136` で 5 ルートすべてを `true` に揃え、修正版を同じ手順で
ビルドし直して **6〜7 件 → 0 件**を確認した。

`[month]` に集中していたのは `monthContentYears()` が `[今年, 翌年]` を
返すためで、2025 年の月別ページ（去年は正当で索引にあった）が全部ここへ
来る。毎年 1 月にこの分だけ増える性質のもの。

**教訓は「疑いを実測に通すまで結論にしない」。**500 だと思い込んだまま
報告していたら、無い問題を直したことになっていた。

### メモリ上限（3 件）— **決着済み（1Gi に上げた。#1139・#1140）**

    Memory limit of 512 MiB exceeded with 512〜513 MiB used

`deploy.yml` は `--cpu=1 --memory=512Mi --max-instances=2
--concurrency=80`。

**落ちるのは 1 リクエストではない。**Cloud Run は上限を超えると
**インスタンスごと強制終了**するので、そのとき処理中だったリクエストは
全部落ちる（`--concurrency=80` なので、運が悪ければ一度に複数人）。
直後はコールドスタートになり、**7 節で「検索に載らない残った有力な
原因」と結論したものを 1 日 3 回自分で増やしている**ことになる。
`--max-instances=2` なので、片方が死ぬともう片方に寄って連鎖しやすい。
掲載データも頁数も増える一方なので、頻度は自然には下がらない。

考えられる手当ては 3 つ。**どれも利用者の判断が要る。**

1. `--memory` を 1Gi に上げる。**課金が増える**（Cloud Run はメモリ×秒）
2. `--concurrency` を下げる。1 インスタンスあたりの同時実行が減るので
   ピークのメモリも下がるが、`--max-instances=2` のままだと待ちが増える
3. 重い頁を軽くする。地図と暦エンジンが乗る画面が候補（11 節に既出）

**まず 3 件がどの経路で出ているかを知りたい。**いまの
`run-error-logs.yml` は `httpRequest` を読まない決めにしてある
（URL・IP・UA が入るため）。`httpRequest.status` と `resource` だけなら
個人は特定できないので、そこだけ足せば経路の当たりは付けられる。
足すかどうかも含めて判断待ち。

---

## 24-b. 費用の正体は us-central1 の置き土産だった（2026-09-09）

**下の 24 節に書いた「37.9 GB ≒ 月 ¥560」は誤りだった。**利用者から
実際の請求（9/03〜9/09 の 7 日）を受け取って判明した。

| プロダクト | 7 日 | 前の期間比 |
| ---------- | ---: | ---------: |
| Artifact Registry | **¥397** | **+24%** |
| Cloud Run | ¥68（正規 ¥106 − 割引 ¥38） | +178% |
| **合計** | **¥466** | （月あたり約 ¥2,000） |

$0.10/GB/月 ＝ 約 ¥0.5/GB/日 で逆算すると**約 113 GB**。測っていた
37.9 GB の 3 倍で、75 GB を数えていなかった。

### 原因は「1 リポジトリしか見ていなかった」

`gar-cleanup.yml` は `REPOSITORY × REGION` を 1 つだけ `describe` して
いて、その数字を「AR の実サイズ」として報告していた。全ロケーションを
横断したら（#1146〜#1148）:

    portfolio-repo  us-central1      101,104 MB  2026-03-08 作成
    portfolio-repo  asia-northeast1   36,457 MB  2026-08-08 作成

**AR の請求の 74% が、誰も使っていないリージョンの置き土産だった。**
クリーンアップポリシーは asia-northeast1 にしか当たっていないので、
us-central1 側は作成以来 1 バイトも消えていなかった。

### 消した（#1149・#1150。2026-09-09 22:35 UTC）

`us-central1-teardown.yml` の apply を実行。実測のログ:

    削除後の us-central1        （空。リポジトリもサービスも無し）
    残す側 asia-northeast1      portfolio-repo  34,099 MB
                                portfolio-app   https://portfolio-app-…-an.a.run.app

削除後に keep-warm を手動で回して success。**本番は無事。**

見込みは月あたり **約 ¥1,700 → 約 ¥435**（実測レート ¥11.9/GB/月）。
次の請求で確かめる。

### この件で 4 回間違えた。並べておく

1. **37.9 GB ≒ 月 ¥560** … 1 リポジトリだけの観測。実際は 142 GB
2. **「掃除は効いていて 38 GB が定常」** … 同じ観測。+24% で増えていた
3. **「us-central1 の残骸は月 ¥84」** … Cloud Run サービス単体の数字。
   しかも**そのサービスは既に無かった**。残っていたのはレジストリだけ
4. **`node_modules` を層に分ければ大きく減る** … 実測 1 割（24 節）

**共通しているのは「一部を測って全体だと言った」こと。**数える範囲を
先に決めて、請求と桁を突き合わせるまで結論にしない。

### 直しの過程でも 3 回落ちた（棚卸しそのもの）

- `--location=-` は `repositories list` では使えない（`--location` を省く）
- `|| true` でそのエラーを握り潰し、**権限の問題に見えた**（`::warning::` に）
- `>> "$GITHUB_STEP_SUMMARY"` だけだと**ログに 1 行も残らない**（`tee` に）

`gcloud run domain-mappings` は 2 回失敗して諦めた（`beta` が runner に
無い / GA は `--region` を取らない）。us-central1 に Cloud Run サービスが
0 件である以上、宛先のないマッピングは存在し得ないので不要と判断した。

---

## 24. 費用の実測と、増え方の手当て（2026-09-08）

利用者から「Artifact Registry が 3,000 近くいっている。構成を考えた
方がいい？」と問われて実測した。

### 実測値（すべて 2026-09-08）

| 何 | 実測 |
| --- | --- |
| Artifact Registry の実サイズ（`repositories describe` の申告） | **37.9 GB** |
| 版数 | 912（最古 7 日前・最新 当日） |
| 層の数 / 100 MB 超 | 3,077 / **308 個 32.3 GB** |
| Cloud Run のリビジョン | **1,055** |
| デプロイ総数 | 1,071 |
| メモリ上限超え | 24 時間で 3 件 |

**掃除は既に効いていた。**`gar-cleanup` の apply は GCP 側の
クリーンアップポリシー（`delete-old` olderThan 604800s ＝ 7 日 /
`keep-recent` keepCount 20）を当てる作りで、最古の版が 7 日前
ちょうどで止まっている。「掃除が回っていないから溜まっている」の
ではなく、**7 日ぶんの定常状態が 38 GB** ということ。

$0.10/GB/月なので **37.9 GB ≒ 月 560 円**。3,000 円が AR 単体なら
この数字と合わない。GCP 全体の月累計か、8 月（128 GB あった頃）の
請求が乗っている可能性が高い。**内訳のどの SKU かは未確認。**

### 何が層を太らせているか

デプロイ 1 回につき圧縮後 111 MB の層が 1 個増える。中身の内訳は
（`next build` の成果物 793 MB を実測）

    /houi/area の事前生成 1,022 頁   554 MB  （70%）
    /houi/2026・2027 ほか            104 MB  （13%）
    node_modules                      79 MB  （10%）
    その他                            56 MB  （ 7%）

**`node_modules` を別の層にしても 1 割しか減らない。**#1141 の見立ては
最初「大きく減る」だったが誤りで、実測して 1 割と分かった。分けること
自体は正しい（依存を変えない限り再利用される）ので入れたが、期待した
効果ではない。

### やったこと

- #1139・#1140 … `--memory=1Gi`（上限超えの手当て）、文書だけの push で
  デプロイしない `paths-ignore`
- #1140 … `gar-cleanup` を週次に（ポリシーの当て直しと容量の記録）、
  Cloud Run の古いリビジョンを整理（配信中と直近 60 個は残す）
- #1141 … `node_modules` を別の層に

### 残っている最大の手（~~判断待ち~~ → **#1152 で実施。2026-09-10**）

利用者の判断（2026-09-09。6 節「費用が下がる／速くなる変更で、機能が
充分に提供できるもの」は聞かずに進めてよい）を受けて、#1152 で
`generateStaticParams` を `areaEditorial` に文章のある頁（344 頁）だけに
した。**812 頁ぶん焼かなくなった。**`dynamicParams = true` なので残りは
要求時に作られ、404 にはならない。見張りは
`__tests__/houiAreaPrerender.test.ts` の 4 件（事前生成した頁は全部
文章あり／文章のある頁は全部事前生成／全件は焼いていない／焼いて
いない頁も `findArea` が引ける）。旧実装（全件）に戻すと 4 件中 3 件が
落ちる。以下は判断の材料として書いたときのまま残す。

**`/houi/area` の 1,022 頁を全部ビルド時に作るのをやめる。**554 MB は
イメージの 7 割で、ここを削れば層も保管料もビルド時間も同時に減る。

#1135 で `dynamicParams = true` にしたので、`generateStaticParams` が
返す件数を減らしても**残りは要求が来たときに作られる**（404 には
ならない）。候補は「`areaEditorial` に文章を書いた頁だけ事前に作る」
（341 頁）。残る約 680 頁は noindex で、DB も暦エンジンも引かず
`areaDirections.json` と静的な import だけで描けるので、その場で
作っても重くない。

**払うもの**は、初回アクセスの遅さ（インスタンスごとに 1 回）と、
ビルド時に落ちるはずの不具合が実行時に回ること。**得るもの**は
イメージ 793 MB → 約 430 MB、層 111 MB → 約 60 MB、保管量ほぼ半分。

見え方が変わる利用者はいない（内容は同じ）が、**性能の性質が変わる**
ので判断を仰ぐ。

---

## 25. 土地の売り出しデータ（**中断。2026-09-10**）

> **結論を先に書く。nifty の規約はスクレイピングを名指しで禁じている。**
> 下の「どこから取るか」で nifty を本命に選んだのは**誤りだった。**
> 利用者が規約を読んで示してくれた（2026-09-10 夜）。
>
> ニフティ不動産サービス特約 第13条（禁止事項の特則）5 号:
>
> > 本不動産サービスに掲載された情報を、個人的な利用範囲を超えて複製、
> > 転載、加工、再配布する行為、またはプログラム等を用いて自動的に
> > データを収集する行為（スクレイピング）。
>
> **何を間違えたか。**「同じホストなので負荷も規約も軽い」と書いた。
> 負荷の話と規約の話は別で、**要求の間隔を空けても、禁止されている行為で
> あることは変わらない。**規約が読めなかったので「人が読むもの」に
> 回したまま、実装を先に進めたのが誤り。**読めないなら進めない。**
>
> **影響は売地だけではない。**同じ規約は `myhome.nifty.com` 全体に
> 掛かるので、毎晩動いている**賃貸の巡回**（`rental_properties` 45 万行、
> 物件検索・裁定スキャナー・市区町村ページの相場）も同じ条項の下にある。
> ここは戻せない判断（データの削除・中心機能の停止）を含むので
> **利用者の指示待ち。**
>
> **止めたもの**（2026-09-10 時点）
>
> - 売地の巡回（`scrape-land.yml`）… 定時起動は元から無い。手で回さない
> - 売地の追加開発（API・画面）… 出どころが決まるまで着手しない
>
> **入っているもの**… 東京 14 区の 1,277 行（座標も埋め済み）。
> 消すかどうかも利用者の判断。
>
> **次に決めること**
>
> 1. 既存の賃貸の巡回を、続ける / 止める / 許諾を取りにいく
> 2. 売地の出どころ（許諾・公式 API・公的データのみ・断念）
> 3. 既に入れた 1,277 行を消すか
>
> **以下は誤った判断に基づく記録として残す。**同じ道を選び直さない
> ための材料であって、手順書ではない。


利用者の要望:「土地の売り出しデータの取得表示もしたい」。

**まだ何も作っていない。**着手の前に決めることがあるので、選択肢と
それぞれの代償をここに置く。

### いま持っているもの（土地の値段は既にある）

| 何 | 表 | 出どころ | 中身 |
| --- | --- | --- | --- |
| 成約価格 | `property_transactions` | 国交省 不動産情報ライブラリ | **いくらで売れたか**（実績） |
| 地価公示・地価調査 | `land_price_points` | 同上（同じ API キー） | **公的な評価額**（地点ごと） |
| 市区町村の平均 | `municipality_wealth.land_price_per_sqm` | 上の集計 | 1.2km 四方の平均 |

取り込みは `scripts/import_land_prices.ts` と
`scripts/import_property_transactions.ts`。**どちらも公式 API なので
相手サーバーへの負荷の話が無い。**

### 足りないのは「いま売りに出ている土地」

上の 3 つは**過去と評価額**で、**現在の在庫ではない。**「この土地が
いくらで買えるか」を出すには、売り出し中の売地の一覧が要る。

**ここが問題で、公的 API に売り出し情報は無い。**不動産情報ライブラリが
持つのは取引価格と地価だけ。

### どこから取るか（調べた結果。2026-09-10）

利用者の問い:「どのサイトがいいですか？HOMES とか？定石はどこ？」

**定石は、上から順に。**下へ行くほど相手への負荷と規約の重さが増す。

1. **公式 API** — 提供側が「機械で読んでよい」と決めた口
2. **提携フィード** — 不動産会社・ポータルが配る一括データ（B2B 契約）
3. **アグリゲータの巡回** — robots.txt と規約の範囲で、間隔を守って読む
4. **ポータルの直接巡回** — robots.txt で断られていることが多い

候補を 1 つずつ当てた。**この環境からはどのポータルの規約ページも
読めない**（出口の proxy が弾く）ので、規約の文面は人が読む。

| 出どころ | 種別 | 売地 | 判定 | 理由 |
| --- | --- | --- | --- | --- |
| **nifty 不動産** `/tochi/` | 3 | 約 21.7 万件 | **本命** | 賃貸の巡回と**同じホスト・同じ URL の形**。SUUMO・HOME'S・アットホームを束ねている。新しいホストへ要求を出さずに済む |
| HOME'S 不動産 API | 1 | あり | **昇格候補** | 公式 API がある（developer.homes.co.jp）。**利用条件が読めていない**ので、人が読んで可なら 3 → 1 へ上げる |
| SUUMO | 4 | あり | 見送り | robots.txt に Disallow が 200 行超、`Crawl-delay: 30`。直接巡回は断られている側 |
| アットホーム | 2 | あり | 見送り | ATBB は不動産会社向けの契約（B2B）。個人サイトでは結べない |
| 国交省 不動産情報ライブラリ | 1 | **無し** | 用途外 | 取引価格と地価だけ。売り出しは持たない（上の表） |
| NII 不動産データセット | — | あり | 見送り | 研究目的限定。公開サイトには使えない |
| 財務省 国有財産・自治体の公有地 | 1〜3 | ごく少数 | 補助 | 件数が桁違いに少ない。**主にはならない** |

**nifty を選ぶ理由は「同じホスト」の 1 点に尽きる。**賃貸で
`MIN_PAGE_INTERVAL_MS = 20000` を守って毎晩回している相手に、
同じ作法でもう 1 系統を足すだけ。**相手を増やさない**のが、負荷と
規約の両方で最も軽い。

    賃貸  https://myhome.nifty.com/rent/{pref}/{city}_ct/{page}/
    売地  https://myhome.nifty.com/tochi/{pref}/{city}_ct/{page}/

**ただし「同じ形で埋め込まれているか」はまだ確かめていない。**賃貸は
`window.Nifty.Data.Bukken` に `rent` / `floorArea` を持つが、売地は
`price` / `landArea` のような別の欄のはず。取り込みを書く前に
**1 頁だけ読んで実物を見る**（下の手順 0）。

### 人が読むもの（この環境からは読めない）

- **nifty 共通利用規約**（niftylifestyle.co.jp/term/）。賃貸の巡回で
  既に前提にしているはずのものだが、**売地に広げる前に改めて読む**
- **HOME'S 不動産 API の利用条件**（developer.homes.co.jp/api）。
  個人サイトで可・件数の上限・表示義務が分かれば、nifty の巡回を
  やめて API へ移せる。**巡回より API のほうが常に上**

### 進め方（着手の単位）

**0. 下見 — 済（2026-09-10 19:49 UTC。run 34522593169、要求 2 回）。**
   `/tochi/tokyo/` の索引に市区町村のリンクが **55 件**あり、URL の形は
   賃貸と同じ。先頭の港区の一覧 1 頁に `Nifty.Data.Bukken` が
   **94 件**入っていた。**同じ形で埋め込まれている。**欄は次のとおり
   （全 94 件に全欄あり）。

   | 欄 | 値の例 | 読み方 |
   | --- | --- | --- |
   | `id` | 64 桁の hex | 賃貸と同じ。頁をまたぐ重複除去の鍵 |
   | `groupId` | 32 桁の hex | **同じ土地を束ねる鍵。**港区の先頭 3 件は SUUMO・ピタットハウス・アットホーム由来の同じ土地で `groupId` が一致した。賃貸で `name_key` を自前で組んでいる名寄せが、売地では相手が済ませている |
   | `url` | `/tochi/tokyo/minatoku_ct/suumof_79174178/` **または** `https://www.pitat.com/buyDetail/…` | **外部サイトへ直接飛ぶものが混ざる。**`@unique` の鍵には使えるが、nifty の相対 URL と決め打ちして絶対化しない |
   | `price` | `"1億5980万円"` **または** `"15,980万円"` | **出どころで書式が違う。**億・万・カンマを全部読む。賃貸の `rent`（万円の小数）とは別の parse |
   | `landArea` | `"44.33㎡（13.40坪）（実測）"` **または** `"44.33m&sup2;"` | 同上。先頭の数値だけ取る。`&sup2;` は HTML 実体のまま来る |
   | `plotRatio` | `"60%"` | **建ぺい率か容積率か、この 1 頁では決められない**（60% は建ぺい率らしいが、容積率も 1 つの値で来るのかが不明）。列名を付ける前に、詳細頁か別の市区町村で 2 つの値が出るかを見る |
   | `type` / `typeName` | `"bes"` / `"土地・売地"` | 種別。売地以外が混ざったらここで弾く |
   | `expireDate` | `"20261010000000"` | 賃貸と同じ形 |
   | `access` / `address` / `title` | 賃貸と同じ | そのまま |
   | `layout` / `floorArea` / `floor` / `story` / `buildAge` | `"-"` | 売地では常に空。読まない |
   | `img` / `urlSmp` / `inquiryUrl` | 画像・スマホ用・問い合わせ | 読まない |

   `rent` / `manageCost` は無い。**緯度経度も無い**ので、賃貸と同じく
   住所から geocode する。

1. **済（#1164）。**`land_listings` 表。DDL は
   `prisma/sql/20260910_add_land_listings.sql`、`db-apply-sql` の dry-run →
   apply で 2026-09-10 20:42 UTC に当てた（run 34527918400。ログに
   `CREATE TABLE` と `CREATE INDEX` × 4、After の一覧に
   `land_listings 56 kB`）。schema.prisma も同じ定義で足した。
   `plotRatio` は `plot_ratio_text` として文字列のまま（建ぺい率か
   容積率か未確定。数値列は意味が分かってから足す）
2. **済（#1165〜#1167）。**相手に対する振る舞い（20 秒・名乗り・索引の
   読み方・期限の読み方）を `scripts/niftyPolite.ts` に出して賃貸と
   売地の両方から読む。欄の読み方と UPSERT は `src/lib/landUpsert.ts`
   （同じ土地の 2 書式が同じ値に落ちることをテストで固定）。取り込みは
   `scripts/nifty_land_extractor.ts`。再開位置はファイルと
   `scraper_state` 表の両方（鍵 `nifty_land`）
   - 座標は `scripts/geocode_properties.ts` と同じ経路。あちらは
     `rental_properties` を SQL に決め打ちしているが、**町丁目 → 座標の
     永続キャッシュ（`geocode_towns`）は表に依らない。**売地の geocode
     はそのキャッシュを先に引き、無い町丁目だけ国土地理院へ行く。
     賃貸の掲載がある町丁目は既にキャッシュにあるので、国土地理院への
     新規の要求は売地しか無い町丁目ぶんだけになる（何件かは
     取り込んでから数える）
3. **済（#1167。ただし手動起動だけ）。**`scrape-land.yml`。schedule は
   置いていない。**巡回の予算と周期は利用者の判断。**決まったら賃貸の
   枠（19:17 UTC 起点で最長 6 時間）と重ならない時間帯に足す。手動で
   回すときも `rental-properties-write` グループで夜間の巡回と重ねない。
   0 件で抜ける経路にも待機を入れてある

   **1 回だけ手で回した実測（2026-09-10 21:25 UTC。run 34531994090。
   東京・予算 5 分・1 市区町村 1 頁）**

   | 見たもの | 結果 |
   | --- | --- |
   | 市区町村の索引 | 55 件 |
   | 回れた市区町村 | 14 区（港・文京・大田・世田谷・足立・千代田・中央・新宿・台東・墨田・江東・品川・目黒・渋谷） |
   | 0 件だった区 | 0 |
   | 書いた行 | 1,277（1 区あたり 29〜132） |
   | 取得の間隔 | 21〜22 秒（20 秒 + 揺らぎ） |
   | 保存の失敗 | 0 |
   | 止まり方 | 予算で渋谷区の 2 頁目を再開位置に残して止まった |

   **東京だけで 55 市区町村 × 数頁**なので、1 頁 20 秒だと東京 1 県の
   一巡に 1 時間半〜数時間かかる。都市圏 12 県を 3 日周期で回すなら
   1 晩 2〜3 時間の枠が要る。この数字を見て予算を決めてもらう
3-b. **次にやること: 座標を埋める。**取り込み直後は `lat` / `lon` が
   NULL で、地図には 1 件も出ない（賃貸と同じ）。`geocode_towns` の
   キャッシュを先に引く小さな geocode を書く
4. 地図の API。`/api/rentals/arbitrage` の経路に混ぜず、**bbox だけを
   受けて `land_listings` を返す軽い口**を 1 つ足す。名寄せも窓関数も
   要らない（売地は 1 件 1 URL）
5. 表示。`/relocation/purchase` に**同じ地図の部品**で載せる
   （`clusterByTile` / `shouldCluster`。地図は増やさない — 3 節）。
   出発地からの方位は `directionFromBearing`、時間軸は
   `vectorsForLayerMode`。**新しく書かない**

### 利用者に決めてもらうこと

- **売地の巡回の予算と周期。**上の「都市圏 12 県・3 日周期・賃貸の
  1/3」は叩き台。CLAUDE.md 6 節で巡回設計は自走しないと決めてある
- **HOME'S API の規約を読めるか。**可なら nifty の巡回の代わりに
  API を使う（巡回より上）
- 表示の置き場所。`/relocation/purchase` に足す前提で進める

## 26. 家賃相場と空き家率を e-Stat から取る（2026-09-11 に着手）

利用者の判断（2026-09-11）。物件の在庫を持つのをやめ、**方位別の相場を
公的データで出し、個別物件はリンクで渡す**方向へ。①家賃相場（e-Stat）
②送客リンク（アフィリエイト）③空き家（→ 空き家率で全自治体）の順。
③の「全部の自治体」は物件の一覧としては無理（全国版空き家バンクは
民間 2 社の運営、自治体のオープンデータは出している所だけ）だが、
**空き家率なら同じ調査・同じ API で全自治体ぶん取れる**ので①と一緒に。

### 規約（利用者が読んで貼ってくれた。nifty のときは写していなかった）

**政府統計の総合窓口（e-Stat）利用規約**（平成 28 年 1 月 29 日、政府標準
利用規約 2.0 準拠、CC BY 4.0 互換）

> 当サイトで公開している情報（以下「コンテンツ」といいます。）は、
> どなたでも以下の１）～６）に従って、複製、公衆送信、翻訳・変形等の
> 翻案等、自由に利用できます。商用利用も可能です。
> また、数値データ、簡単な表・グラフ等は著作権の対象ではありませんので、
> これらについては本利用ルールの適用はなく、自由に利用できます。

> １）ア コンテンツを利用する際は出典を記載してください。
> 　　（出典記載例）出典：政府統計の総合窓口(e-Stat)（https://www.e-stat.go.jp/）
> 　　イ コンテンツを編集・加工等して利用する場合は、上記出典とは別に、
> 　　編集・加工等を行ったことを記載してください。なお、編集・加工した
> 　　情報を、あたかも国（又は府省等）が作成したかのような態様で
> 　　公表・利用してはいけません。

別紙で「ＡＰＩ機能の利用について」は別の利用ルール。その中のクレジット:

> API機能を使用したサービスを公開される場合には、下記のクレジット表示を
> お願いします。
> 「このサービスは、政府統計総合窓口(e-Stat)のAPI機能を使用していますが、
> サービスの内容は国によって保証されたものではありません。」
> 利用される方が参照できる場所であれば、表示場所の指定はございません。

API の規約は、利用者が貼ってくれたクレジットの条項以外は**未読のまま
回した**（2026-09-11）。根拠は、データの利用条件（上の規約全文。CC BY
互換・商用可）が読めていること、クレジットは実装済みであること、
富裕度が既に同じ API・同じ appId で動いていること、要求が 1 回で済む
こと。5 年に 1 回の取り込みなので頻度の条項に触れる余地はほぼ無い。
API のページを読める機会があれば全文を写す。

### 守ること

1. 出典: 「出典：住宅・土地統計調査（総務省）／政府統計の総合窓口(e-Stat)」
2. 加工の明記: 方位別に集計し直すので「〜を加工して作成」
3. API のクレジット文をそのまま 1 行（札の下）
4. 国が作成したかのような態様にしない（#1174 と同じ）

### 見つかった不具合

**e-Stat は既に本番で使われていた**（`/relocation/wealth` の富裕度。
`import_municipalities_wealth.ts` が `ESTAT_APP_ID` で `0000020103` を
取っている）のに、**API のクレジット表示がサイトのどこにも無い**
（`grep -rn "e-Stat" src/app src/components` が 0 件）。富裕度の出典欄に
足す（1 ファイルの PR）。`ESTAT_APP_ID` は `ENV_FILE` に入っているので
登録は不要。

### 進み方（2026-09-11 深夜。コード側は本番まで揃った）

| 段 | PR | 状態 |
|---|---|---|
| 探る | #1192・#1194（`probe_estat_housing.ts`） | 表 `0000020108`（Ｈ 居住）に 5 項目があると実物で確認（run 34649544727） |
| 表 | #1195（`municipality_housing_stats`） | **本番に適用済み**（dry-run 34650757307 → apply 34650818632。after の一覧に 24 kB） |
| 取り込み | #1196（`estatHousing.ts` + テスト 5 件、`import_estat_housing.ts`）・#1197（`import-estat-housing.yml`） | **回した**（2026-09-11 23:11 UTC、run 34657021102、2023 年）。市区町村 **1,235** 件、うち 1 畳当たり家賃あり **991**、空き家率を出せる **1,235**。要求 1 回・8 秒。町村の一部は住調の対象外で 1,900 弱にはならない |
| 口 | #1198（`lib/housingStatsDirections` + テスト 6 件、`/api/housing-stats/by-direction`）・#1200（入口のテスト 3 件） | マージ・deploy 済み |
| 札 | #1199（`HousingStatsByDirection`。物件検索の購入モードで地価の札の隣） | マージ・deploy 済み。表が空のあいだは「統計のある市区町村がありませんでした」 |
| 不具合 | #1193（富裕度の出典欄に API のクレジットが無かった） | マージ・deploy 済み。見張りは `estatApiCredit.test.ts` |

**単位の決め。**「1 か月当たり家賃」の市区町村別の平均は e-Stat に無い
（住調の表は 10 区分の戸数＝分布）。**1 畳当たり家賃 ÷ 1.62 で円/㎡・月**
にして、地価（万円/㎡）や物件検索の㎡単価と同じ単位で並べる。月額は
1 畳当たり家賃 × 借家の平均畳数で、平均どうしの積なので**目安**と書く。
空き家率は方位内の合計どうしの比（戸数で重み付け）。

**「全部の自治体」の答え。**空き家バンクの一覧は全国 1 つのデータに
なっていない（全国版は民間 2 社の運営、自治体のオープンデータは出して
いる所だけ）。空き家率なら同じ調査で全自治体ぶん取れるので、①と一緒に
した（利用者の判断:「空き家率も一緒に入れて進めて」）。

### 手順（地価公示と同じ: 探る → 表 → 取り込み → 口 → 札）

1. `scripts/probe_estat_housing.ts`（読むだけ）で `0000020103` の項目から
   「1か月当たり家賃」「空き家数」「住宅総数」の**コードを実物で確かめる**。
   推測で書くと「1 畳当たり家賃」を家賃として取り込んでも気付けない
   （probe_land_prices と同じ理由）。無ければ住調の市区町村別の表を探す
2. 表（足すだけの DDL）と `schema.prisma`
3. 取り込み（富裕度と同じ作り。年度は 1 か所で決める）
4. `/api/housing-stats/by-direction`（`landPricesByDirection` と同じ形。
   代表点からの方位で市区町村を束ねる）
5. `LandPriceByDirection` の隣に札。出典・加工・クレジットを下に

